---
phase: 04-multi-source-expansion
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/lib/scraper/adapters/sam-gov-bids.ts
  - src/lib/scraper/adapters/enr-news.ts
  - src/lib/scraper/adapters/construction-dive-news.ts
  - src/lib/scraper/adapters/prnewswire-news.ts
  - src/lib/scraper/adapters/google-dorking.ts
  - src/lib/scraper/adapters/index.ts
  - tests/scraper/sam-gov-adapter.test.ts
  - tests/scraper/news-adapter.test.ts
  - tests/scraper/dorking-adapter.test.ts
autonomous: true
requirements:
  - DATA-02
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - "SAM.gov adapter fetches federal construction bid opportunities and returns valid RawLeadData with sourceType=bid"
    - "RSS news adapters parse ENR, Construction Dive, and PR Newswire feeds and return valid RawLeadData with sourceType=news"
    - "Google dorking adapter sends construction-specific queries to Serper.dev and returns valid RawLeadData with sourceType=deep-web"
    - "All new adapters gracefully skip when their API keys are missing (no crashes)"
    - "All new adapters are registered in initializeAdapters() and run with the daily scheduler"
  artifacts:
    - path: "src/lib/scraper/adapters/sam-gov-bids.ts"
      provides: "SAM.gov federal bid board adapter"
      exports: ["SamGovBidsAdapter"]
    - path: "src/lib/scraper/adapters/enr-news.ts"
      provides: "ENR construction news RSS adapter"
      exports: ["EnrNewsAdapter"]
    - path: "src/lib/scraper/adapters/construction-dive-news.ts"
      provides: "Construction Dive news RSS adapter"
      exports: ["ConstructionDiveNewsAdapter"]
    - path: "src/lib/scraper/adapters/prnewswire-news.ts"
      provides: "PR Newswire construction press release RSS adapter"
      exports: ["PrNewswireNewsAdapter"]
    - path: "src/lib/scraper/adapters/google-dorking.ts"
      provides: "Serper.dev Google dorking adapter"
      exports: ["GoogleDorkingAdapter"]
    - path: "tests/scraper/sam-gov-adapter.test.ts"
      provides: "SAM.gov adapter unit tests"
    - path: "tests/scraper/news-adapter.test.ts"
      provides: "RSS news adapter unit tests"
    - path: "tests/scraper/dorking-adapter.test.ts"
      provides: "Google dorking adapter unit tests"
  key_links:
    - from: "src/lib/scraper/adapters/sam-gov-bids.ts"
      to: "src/lib/scraper/adapters/base-adapter.ts"
      via: "implements ScraperAdapter, returns RawLeadData[]"
      pattern: "implements ScraperAdapter"
    - from: "src/lib/scraper/adapters/enr-news.ts"
      to: "rss-parser"
      via: "Parser.parseURL for RSS feed fetching"
      pattern: "parser\\.parseURL"
    - from: "src/lib/scraper/adapters/google-dorking.ts"
      to: "https://google.serper.dev/search"
      via: "POST request with X-API-KEY header"
      pattern: "serper\\.dev"
    - from: "src/lib/scraper/adapters/index.ts"
      to: "all adapter files"
      via: "registerAdapter() calls in initializeAdapters()"
      pattern: "registerAdapter"

user_setup:
  - service: SAM.gov
    why: "Federal bid board data (DATA-02)"
    env_vars:
      - name: SAM_GOV_API_KEY
        source: "Register at https://sam.gov/content/entity-registration then request API key at https://open.gsa.gov/api/get-opportunities-public-api/ -- NOTE: approval takes 1-4 weeks"
    dashboard_config: []
  - service: Serper.dev
    why: "Google dorking / deep web search (DATA-04)"
    env_vars:
      - name: SERPER_API_KEY
        source: "Sign up at https://serper.dev -- free tier includes 2,500 queries"
    dashboard_config: []
---

<objective>
Create three new scraper adapter types: SAM.gov bid board, RSS construction news (3 feeds), and Google dorking via Serper.dev.

Purpose: Expand lead pipeline from permits-only to four source types (permits, bids, news, deep-web), directly addressing DATA-02, DATA-03, and DATA-04.
Output: 5 new adapter classes, all registered for daily pipeline execution, with unit tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-source-expansion/04-RESEARCH.md
@.planning/phases/04-multi-source-expansion/04-01-SUMMARY.md

@src/lib/scraper/adapters/base-adapter.ts
@src/lib/scraper/adapters/austin-permits.ts
@src/lib/scraper/adapters/index.ts
@src/lib/scraper/registry.ts

<interfaces>
<!-- From Plan 01 (must be completed first) -->
<!-- These are the generalized contracts that new adapters implement -->

From src/lib/scraper/adapters/base-adapter.ts (post-Plan-01):
```typescript
export const rawLeadSchema = z.object({
  permitNumber: z.string().optional(),
  title: z.string().optional(),
  externalId: z.string().optional(),
  description: z.string().optional(),
  address: z.string().optional(),
  projectType: z.string().optional(),
  estimatedValue: z.number().optional(),
  applicantName: z.string().optional(),
  contractorName: z.string().optional(),
  agencyName: z.string().optional(),
  permitDate: z.coerce.date().optional(),
  postedDate: z.coerce.date().optional(),
  deadlineDate: z.coerce.date().optional(),
  lat: z.number().optional(),
  lng: z.number().optional(),
  city: z.string().optional(),
  state: z.string().optional(),
  sourceUrl: z.string().optional(),
  sourceType: z.enum(["permit", "bid", "news", "deep-web"]),
}).refine(/* at least one identity field */);

export type RawLeadData = z.infer<typeof rawLeadSchema>;

export interface ScraperAdapter {
  readonly sourceId: string;
  readonly sourceName: string;
  readonly sourceType: "permit" | "bid" | "news" | "deep-web";
  readonly jurisdiction?: string;
  scrape(): Promise<RawLeadData[]>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SAM.gov bid board and RSS news adapters</name>
  <files>
    src/lib/scraper/adapters/sam-gov-bids.ts
    src/lib/scraper/adapters/enr-news.ts
    src/lib/scraper/adapters/construction-dive-news.ts
    src/lib/scraper/adapters/prnewswire-news.ts
    tests/scraper/sam-gov-adapter.test.ts
    tests/scraper/news-adapter.test.ts
  </files>
  <behavior>
    SAM.gov adapter:
    - Scrapes construction opportunities using NAICS codes 236, 237, 238
    - Maps SAM.gov response fields to RawLeadData (title, description, agencyName, postedDate, deadlineDate, city, state, externalId=noticeId, sourceType="bid")
    - Returns empty array (no crash) when SAM_GOV_API_KEY env var is missing
    - Returns empty array on API error (non-200 response)
    - Filters to last 30 days of posted opportunities
    - Each result has sourceType="bid" and a valid externalId

    RSS news adapters (ENR, Construction Dive, PR Newswire):
    - Parses RSS feed items into RawLeadData with sourceType="news"
    - Maps RSS item fields: title -> title, contentSnippet -> description, link -> sourceUrl, pubDate -> postedDate
    - Filters items for construction-relevance using keyword matching (construction, building, project, groundbreaking, equipment, excavat, crane, etc.)
    - Extracts city/state from article text using regex pattern matching ("in City, State" or "City, ST")
    - Returns empty array on feed parse error (no crash)
    - Each result has externalId set to the article URL for dedup
  </behavior>
  <action>
    Install dependencies first: `npm install rss-parser && npm install -D @types/rss-parser` (if types exist — rss-parser includes its own types, so @types may not be needed; check and skip if unnecessary).

    1. Create src/lib/scraper/adapters/sam-gov-bids.ts:
       - Class SamGovBidsAdapter implements ScraperAdapter
       - sourceId="sam-gov-bids", sourceName="SAM.gov Federal Contract Opportunities", sourceType="bid"
       - endpoint: "https://api.sam.gov/opportunities/v2/search"
       - NAICS codes: ["236", "237", "238"] (building, heavy/civil, specialty trade construction)
       - scrape() method:
         * Check process.env.SAM_GOV_API_KEY — if missing, log warning and return []
         * Query last 30 days with each NAICS code
         * URL params: api_key, postedFrom (MM/dd/yyyy format), postedTo, ncode, limit=100
         * Map response opportunitiesData array items to RawLeadData:
           - title: item.title
           - description: item.description
           - agencyName: item.department || item.subtier
           - postedDate: item.postedDate
           - deadlineDate: item.responseDeadLine
           - city: item.officeAddress?.city
           - state: item.officeAddress?.state
           - externalId: item.noticeId
           - sourceUrl: `https://sam.gov/opp/${item.noticeId}/view`
           - sourceType: "bid"
           - estimatedValue: parse from item.award?.amount if present
         * Wrap each NAICS query in try/catch, continue on error
         * Set Accept header to "application/json"

    2. Create a shared helper function `extractLocation(text: string): { city?: string; state?: string }` at the top of the first news adapter file (or in a shared utils). Uses regex patterns:
       - Pattern 1: "in (City), (ST)" where ST is 2-letter state code
       - Pattern 2: "in (City), (State Name)" where State Name is full name
       - Pattern 3: "(City), (ST)" at sentence boundaries
       - Returns first match or empty object

    3. Create a shared helper function `isConstructionRelevant(title?: string, description?: string): boolean`. Returns true if title or description contains any of: construction, building, project, groundbreaking, equipment, excavat, crane, contractor, infrastructure, demolition, renovation, commercial, residential, industrial, highway, bridge, utility.

    4. Create src/lib/scraper/adapters/enr-news.ts:
       - Class EnrNewsAdapter implements ScraperAdapter
       - sourceId="enr-news", sourceName="Engineering News-Record", sourceType="news"
       - feedUrls: ["https://www.enr.com/rss/1", "https://www.enr.com/rss/11", "https://www.enr.com/rss/9"]
       - scrape() with rss-parser: parse each feed, filter by isConstructionRelevant, map items to RawLeadData with extractLocation on title+description, externalId=item.link
       - Set Accept header on rss-parser requests: "application/rss+xml, application/xml"
       - Wrap each feed URL in try/catch, continue on parse errors

    5. Create src/lib/scraper/adapters/construction-dive-news.ts:
       - Class ConstructionDiveNewsAdapter, same pattern as ENR
       - sourceId="construction-dive-news", sourceName="Construction Dive", sourceType="news"
       - feedUrls: ["https://www.constructiondive.com/feeds/news/"]

    6. Create src/lib/scraper/adapters/prnewswire-news.ts:
       - Class PrNewswireNewsAdapter, same pattern
       - sourceId="prnewswire-news", sourceName="PR Newswire Construction", sourceType="news"
       - feedUrls: ["https://www.prnewswire.com/rss/construction-building-news.rss"]

    7. Create tests/scraper/sam-gov-adapter.test.ts:
       - Mock global fetch to return fake SAM.gov response data
       - Test: returns valid RawLeadData with sourceType="bid" from mocked response
       - Test: returns empty array when SAM_GOV_API_KEY is not set
       - Test: returns empty array on fetch error (non-200)
       - Test: each result has externalId (noticeId) set

    8. Create tests/scraper/news-adapter.test.ts:
       - Mock rss-parser to return fake feed items
       - Test: filters non-construction items (returns only relevant ones)
       - Test: maps RSS fields correctly (title, sourceUrl, postedDate, sourceType="news")
       - Test: extracts city/state from text with "in Dallas, TX" pattern
       - Test: returns empty array on parse error
       - Test: each result has externalId set to article URL
  </action>
  <verify>
    <automated>npx vitest run tests/scraper/sam-gov-adapter.test.ts tests/scraper/news-adapter.test.ts --reporter=verbose</automated>
  </verify>
  <done>SAM.gov adapter maps API response to RawLeadData with sourceType="bid", all 3 RSS adapters parse feeds with construction filtering and location extraction, all adapters gracefully handle missing API keys and errors, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Google dorking adapter and adapter registration</name>
  <files>
    src/lib/scraper/adapters/google-dorking.ts
    src/lib/scraper/adapters/index.ts
    tests/scraper/dorking-adapter.test.ts
  </files>
  <behavior>
    Google dorking adapter:
    - Sends construction-specific dork queries to Serper.dev API
    - Returns valid RawLeadData with sourceType="deep-web" for each search result
    - Only stores search result metadata (title, snippet, URL) -- does NOT follow links to scrape third-party sites (legal safety per STATE.md concern)
    - Returns empty array when SERPER_API_KEY env var is missing
    - Rate limits queries to a configurable daily budget (default 50 queries/day)
    - Rotates through query templates, not running all every day
    - Each result has externalId set to the result URL

    Registration:
    - All 5 new adapters registered in initializeAdapters()
    - Daily scheduler auto-runs all registered adapters (no scheduler changes needed)
  </behavior>
  <action>
    1. Create src/lib/scraper/adapters/google-dorking.ts:
       - Class GoogleDorkingAdapter implements ScraperAdapter
       - sourceId="google-dorking", sourceName="Google Deep Web Search", sourceType="deep-web"
       - endpoint: "https://google.serper.dev/search"
       - Define query templates array (5-10 dorking queries):
         * 'site:linkedin.com/jobs "heavy equipment" OR "construction equipment" operator'
         * '"groundbreaking ceremony" construction project 2026'
         * '"contract awarded" construction "million" -news'
         * 'filetype:pdf "request for proposal" construction equipment'
         * 'intitle:"bid results" construction heavy equipment'
         * '"equipment rental" "new location" OR "expanding" construction'
         * '"project approved" construction commercial residential'
       - DAILY_QUERY_BUDGET constant = 50 (configurable)
       - scrape() method:
         * Check process.env.SERPER_API_KEY — if missing, log warning and return []
         * Determine which queries to run today: use (dayOfYear % totalQueries) as starting index, run up to DAILY_QUERY_BUDGET queries. This rotates through the full query list over multiple days.
         * For each query: POST to Serper endpoint with headers {"X-API-KEY": key, "Content-Type": "application/json"}, body: {q: query, num: 20}
         * Map response.organic array to RawLeadData:
           - title: result.title
           - description: result.snippet
           - sourceUrl: result.link
           - externalId: result.link (URL as unique identifier)
           - sourceType: "deep-web"
           - postedDate: result.date ? new Date(result.date) : undefined
         * Extract city/state from snippets using the same extractLocation helper from Task 1 (import it — move the helper to a shared location if needed, e.g. src/lib/scraper/adapters/utils.ts)
         * Wrap each query in try/catch, continue on error
         * Add 100ms delay between Serper API calls to be respectful

    2. If extractLocation and isConstructionRelevant were defined inline in a news adapter in Task 1, refactor them to src/lib/scraper/adapters/utils.ts and import from there in both the news adapters and google-dorking adapter.

    3. Update src/lib/scraper/adapters/index.ts:
       - Import all 5 new adapter classes
       - Register each in initializeAdapters():
         * registerAdapter(new SamGovBidsAdapter())
         * registerAdapter(new EnrNewsAdapter())
         * registerAdapter(new ConstructionDiveNewsAdapter())
         * registerAdapter(new PrNewswireNewsAdapter())
         * registerAdapter(new GoogleDorkingAdapter())
       - No changes to scheduler.ts needed — it calls initializeAdapters() which now includes the new adapters

    4. Create tests/scraper/dorking-adapter.test.ts:
       - Mock global fetch to return fake Serper.dev response
       - Test: returns valid RawLeadData with sourceType="deep-web" from mocked response
       - Test: returns empty array when SERPER_API_KEY is not set
       - Test: each result has externalId set to result URL
       - Test: only stores metadata (title, snippet, URL) — no link-following behavior
       - Test: respects daily query budget (does not exceed DAILY_QUERY_BUDGET queries)
  </action>
  <verify>
    <automated>npx vitest run tests/scraper/dorking-adapter.test.ts tests/scraper/ --reporter=verbose</automated>
  </verify>
  <done>Google dorking adapter sends queries to Serper.dev and returns RawLeadData, stores only search metadata (no link scraping), all 5 new adapters registered in initializeAdapters(), daily query rotation works, all tests pass, full scraper test suite green</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/scraper/ --reporter=verbose` — all scraper tests green (existing + new)
- `npx vitest run --reporter=verbose` — full test suite green
- Manual check (optional, requires API keys): Set SAM_GOV_API_KEY and SERPER_API_KEY env vars, run a manual pipeline trigger via POST /api/scraper/run, verify bid and deep-web leads appear in DB
</verification>

<success_criteria>
- SAM.gov adapter fetches federal bid opportunities by NAICS code and maps to RawLeadData
- Three RSS news adapters (ENR, Construction Dive, PR Newswire) parse feeds with construction keyword filtering
- Google dorking adapter queries Serper.dev with rotating construction dork queries within daily budget
- All adapters gracefully return [] when API keys are missing
- All adapters handle errors without crashing the pipeline
- All 5 new adapters registered in initializeAdapters() for daily pipeline execution
- Location extraction from article/snippet text provides city/state when detectable
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-source-expansion/04-02-SUMMARY.md`
</output>
