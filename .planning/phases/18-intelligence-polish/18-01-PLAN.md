---
phase: 18-intelligence-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/scraper/adapters/socrata-violation-adapter.ts
  - src/lib/scraper/adapters/austin-violations.ts
  - src/lib/scraper/adapters/dallas-violations.ts
  - src/lib/scraper/adapters/houston-violations.ts
  - src/lib/scraper/adapters/eia-utility-rates.ts
  - src/lib/scraper/adapters/solar-incentives.ts
  - src/lib/scraper/adapters/index.ts
  - src/lib/scraper/adapters/base-adapter.ts
  - src/lib/scraper/api-rate-limiter.ts
  - tests/scraper/violation-adapter.test.ts
  - tests/scraper/eia-adapter.test.ts
  - tests/scraper/solar-incentives.test.ts
  - tests/scraper/factory.test.ts
autonomous: true
requirements:
  - SCRP-08
  - SCRP-09
  - SCRP-10

must_haves:
  truths:
    - "Code violation leads are scraped from 2-3 Socrata cities and tagged for HVAC/roofing/electrical industries"
    - "EIA utility rate data is fetched and stored as lead enrichments for solar ROI context"
    - "Top 15 state solar incentive programs are queryable as a lookup table"
    - "New adapters are registered in the factory and returned for their respective industries"
  artifacts:
    - path: "src/lib/scraper/adapters/socrata-violation-adapter.ts"
      provides: "Abstract base class for code violation Socrata datasets"
      min_lines: 60
    - path: "src/lib/scraper/adapters/austin-violations.ts"
      provides: "Austin code violations adapter"
      min_lines: 30
    - path: "src/lib/scraper/adapters/dallas-violations.ts"
      provides: "Dallas code violations adapter"
      min_lines: 30
    - path: "src/lib/scraper/adapters/houston-violations.ts"
      provides: "Houston code violations adapter"
      min_lines: 30
    - path: "src/lib/scraper/adapters/eia-utility-rates.ts"
      provides: "EIA API adapter for utility rate data"
      min_lines: 60
    - path: "src/lib/scraper/adapters/solar-incentives.ts"
      provides: "Static lookup table of top 15 state solar incentive programs"
      min_lines: 80
    - path: "src/lib/scraper/adapters/index.ts"
      provides: "Factory updated with violation/EIA/solar adapters"
  key_links:
    - from: "src/lib/scraper/adapters/socrata-violation-adapter.ts"
      to: "src/lib/scraper/adapters/socrata-permit-adapter.ts"
      via: "Extends same SocrataPermitAdapter base or parallel abstract class using same SODA3/SODA2 pattern"
      pattern: "getSocrataQueue"
    - from: "src/lib/scraper/adapters/index.ts"
      to: "src/lib/scraper/adapters/austin-violations.ts"
      via: "Factory returns violation adapters for hvac/roofing/electrical"
      pattern: "getAdaptersForIndustry"
    - from: "src/lib/scraper/adapters/eia-utility-rates.ts"
      to: "src/lib/scraper/api-rate-limiter.ts"
      via: "Uses dedicated EIA rate limiter queue"
      pattern: "getEiaQueue"
---

<objective>
Build the three industry intelligence data sources: code violation scrapers for HVAC/roofing/electrical leads, EIA utility rate scraper for solar ROI context, and a curated solar incentive lookup table.

Purpose: These data sources provide industry-specific intelligence that differentiates LeadForge from generic lead platforms. Code violations signal repair/upgrade demand for HVAC/roofing/electrical contractors. Utility rates and solar incentives provide ROI context that solar installers need to close deals.

Output: 6 new adapter files, updated factory, updated rate limiter, and comprehensive tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/scraper/adapters/socrata-permit-adapter.ts
@src/lib/scraper/adapters/base-adapter.ts
@src/lib/scraper/adapters/index.ts
@src/lib/scraper/adapters/austin-permits.ts
@src/lib/scraper/api-rate-limiter.ts
@src/lib/scraper/content-hash.ts
@src/lib/scraper/pipeline.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/scraper/adapters/base-adapter.ts:
```typescript
export const sourceTypes = ["permit", "bid", "news", "deep-web"] as const;
export type SourceType = (typeof sourceTypes)[number];

export interface ScraperAdapter {
  readonly sourceId: string;
  readonly sourceName: string;
  readonly sourceType: SourceType;
  readonly jurisdiction?: string;
  scrape(): Promise<RawLeadData[]>;
}
```

From src/lib/scraper/adapters/socrata-permit-adapter.ts:
```typescript
export interface SocrataConfig {
  sourceId: string;
  sourceName: string;
  jurisdiction: string;
  domain: string;
  datasetId: string;
  fieldMap: {
    permitNumber: string;
    description?: string;
    address: string;
    projectType?: string;
    estimatedValue?: string;
    applicantName?: string;
    permitDate: string;
    latitude?: string;
    longitude?: string;
  };
}

export abstract class SocrataPermitAdapter implements ScraperAdapter {
  // SODA3 POST with SODA2 GET fallback, rate limited via getSocrataQueue()
  async scrape(): Promise<RawLeadData[]>;
  protected abstract mapRecords(data: Record<string, unknown>[]): RawLeadData[];
}
```

From src/lib/scraper/api-rate-limiter.ts:
```typescript
export async function getSocrataQueue(): Promise<PQueue>;
export async function getSamGovQueue(): Promise<PQueue>;
```

From src/lib/scraper/adapters/index.ts:
```typescript
export function getAdaptersForIndustry(industry: Industry): ScraperAdapter[];
export function getAllAdapters(): ScraperAdapter[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Code violation Socrata adapters for 3 cities</name>
  <files>
    src/lib/scraper/adapters/base-adapter.ts
    src/lib/scraper/adapters/socrata-violation-adapter.ts
    src/lib/scraper/adapters/austin-violations.ts
    src/lib/scraper/adapters/dallas-violations.ts
    src/lib/scraper/adapters/houston-violations.ts
    src/lib/scraper/adapters/index.ts
    src/lib/scraper/content-hash.ts
    tests/scraper/violation-adapter.test.ts
    tests/scraper/factory.test.ts
  </files>
  <behavior>
    - Test: SocrataViolationAdapter fetches from SODA3 endpoint and falls back to SODA2, using the Socrata rate limiter queue
    - Test: Austin violations adapter maps records correctly (violation_number -> title, violation_description -> description, case_type categorizes for HVAC/roofing/electrical)
    - Test: Dallas violations adapter maps records with different field names from Dallas dataset
    - Test: Houston violations adapter maps records from Houston Socrata portal
    - Test: All violation adapters have sourceType "violation" (not "permit")
    - Test: getAdaptersForIndustry("hvac") includes violation adapters; "heavy_equipment" does not
    - Test: getAdaptersForIndustry("roofing") and "electrical" include violation adapters
    - Test: getAllAdapters() returns the superset including violation adapters
  </behavior>
  <action>
    1. Add "violation" to sourceTypes array in base-adapter.ts: `["permit", "bid", "news", "deep-web", "violation"]`

    2. Create `socrata-violation-adapter.ts` -- an abstract base class similar to SocrataPermitAdapter but for code violation datasets:
       - Accepts a SocrataViolationConfig (similar to SocrataConfig but with violation-specific field mapping: violationNumber, description, address, caseType/violationCode, dateField, latitude, longitude, status)
       - sourceType = "violation" (not "permit")
       - Uses getSocrataQueue() for rate limiting (same Socrata rate limiter -- violations are also on Socrata)
       - SODA3 POST with SODA2 GET fallback (reuse the same pattern from SocrataPermitAdapter)
       - Query filter: fetch violations from last 60 days (longer window than permits since violations persist)
       - Abstract mapRecords() method for city-specific field mapping
       - Map violation records to RawLeadData with: title = violation type/code, description = full violation text, sourceType = "violation", permitDate = violation date

    3. Create `austin-violations.ts`:
       - Dataset: City of Austin Code Complaints -- use domain "data.austintexas.gov"
       - Research the actual Socrata dataset ID for Austin code violations (likely "4tni-rr7x" or similar code complaint dataset)
       - Map: case_type/violation_type -> projectType, description -> description, address -> address
       - Tag applicableIndustries based on case_type: "HVAC" keywords -> ["hvac"], "Roof" keywords -> ["roofing"], "Electrical" keywords -> ["electrical"], general building -> all three
       - sourceId: "austin-tx-violations"

    4. Create `dallas-violations.ts`:
       - Dataset: City of Dallas Code Compliance -- domain "www.dallasopendata.com"
       - sourceId: "dallas-tx-violations"
       - Same pattern: map Dallas-specific field names to RawLeadData

    5. Create `houston-violations.ts`:
       - Dataset: City of Houston Code Enforcement -- domain "data.houstontx.gov"
       - sourceId: "houston-tx-violations"
       - Same pattern

    6. Update content-hash.ts: Add "violation" case that hashes on "sourceId:violationNumber" (same pattern as permit).

    7. Update `adapters/index.ts` factory:
       - Import all three violation adapters
       - Add violation adapters to hvac, roofing, and electrical industry arrays
       - Do NOT add to heavy_equipment or solar (violations are not relevant to those industries)
       - Update getAllAdapters() to include violation adapters in the superset

    8. Update existing factory.test.ts to verify violation adapters are returned for correct industries and absent for heavy_equipment/solar.
  </action>
  <verify>
    <automated>npx vitest run tests/scraper/violation-adapter.test.ts tests/scraper/factory.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    Three code violation Socrata adapters (Austin, Dallas, Houston) scrape violation data, map to RawLeadData with sourceType "violation", use shared Socrata rate limiter, and are registered in the factory for HVAC/roofing/electrical industries. "violation" is a valid source type. All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EIA utility rate adapter and solar incentive lookup table</name>
  <files>
    src/lib/scraper/adapters/eia-utility-rates.ts
    src/lib/scraper/adapters/solar-incentives.ts
    src/lib/scraper/api-rate-limiter.ts
    src/lib/scraper/adapters/index.ts
    tests/scraper/eia-adapter.test.ts
    tests/scraper/solar-incentives.test.ts
  </files>
  <behavior>
    - Test: EiaUtilityRateAdapter fetches state-level residential electricity rates from EIA API v2
    - Test: EIA adapter uses a dedicated rate limiter queue (getEiaQueue), not the Socrata queue
    - Test: EIA adapter maps response to RawLeadData with sourceType "news" (reusing existing type for non-permit enrichment data) or a dedicated enrichment storage approach
    - Test: EIA adapter stores rate data as lead enrichment records (enrichmentType: "utility_rate")
    - Test: Solar incentive lookup returns top 15 state programs with state, program_name, incentive_type, description, max_value, and url
    - Test: getSolarIncentives(state) returns incentives for a given state (e.g., "CA" returns California programs)
    - Test: getSolarIncentives("XX") returns empty array for unknown states
    - Test: getAdaptersForIndustry("solar") includes EIA adapter
  </behavior>
  <action>
    1. Add EIA rate limiter to api-rate-limiter.ts:
       - `getEiaQueue()`: concurrency 1, 30 req/min (EIA API is generous but be conservative)
       - Same lazy-init pattern as existing queues

    2. Create `eia-utility-rates.ts` -- EiaUtilityRateAdapter:
       - Implements ScraperAdapter interface
       - sourceId: "eia-utility-rates", sourceName: "EIA Residential Electricity Rates"
       - sourceType: "news" (reuse existing type -- utility rates are informational data, not leads themselves)
       - Fetches from EIA Open Data API v2: `https://api.eia.gov/v2/electricity/retail-sales/data/`
       - Query params: frequency=annual, data[]=price, facets[sectorid][]=RES (residential), sort[0][column]=period&sort[0][direction]=desc, length=100
       - Requires EIA_API_KEY env var (.trim() it!)
       - Maps response: each state record -> RawLeadData with title = "{state} Residential Electricity Rate: {price} cents/kWh", description = context about solar ROI, city = state capital, state = state abbreviation
       - Uses getEiaQueue() for rate limiting
       - If EIA_API_KEY not set, log warning and return empty array (graceful degradation)

    3. Create `solar-incentives.ts` -- NOT a ScraperAdapter (static data, no API):
       - Export const SOLAR_INCENTIVES: SolarIncentive[] with 15 entries for top states
       - Type: `{ state: string; stateCode: string; programName: string; incentiveType: "tax_credit" | "rebate" | "net_metering" | "srec" | "grant"; description: string; maxValue: string; url: string; lastUpdated: string }`
       - Include programs for: CA, TX, NY, FL, AZ, MA, NJ, CO, NC, CT, MD, MN, IL, NV, OR
       - Data sourced from DSIRE database (manually curated, accurate as of early 2026)
       - Export function `getSolarIncentives(stateCode: string): SolarIncentive[]` -- returns matching incentives for a state
       - Export function `getAllSolarIncentives(): SolarIncentive[]` -- returns all 15

    4. Update `adapters/index.ts`:
       - Import EiaUtilityRateAdapter
       - Add to solar industry: `new EiaUtilityRateAdapter()`
       - Do NOT add solar incentives to factory (it's a lookup table, not a scraper adapter)

    5. Write comprehensive tests:
       - tests/scraper/eia-adapter.test.ts: Mock fetch to test EIA response parsing, rate limiting, graceful degradation on missing API key
       - tests/scraper/solar-incentives.test.ts: Test lookup function returns correct incentives per state, empty for unknown states, all 15 entries in full list
  </action>
  <verify>
    <automated>npx vitest run tests/scraper/eia-adapter.test.ts tests/scraper/solar-incentives.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    EIA utility rate adapter fetches state-level electricity prices via EIA API v2 with dedicated rate limiting and maps to lead data for solar ROI context. Solar incentive lookup table provides curated top 15 state programs queryable by state code. EIA adapter registered in factory for solar industry. All tests pass.
  </done>
</task>

</tasks>

<verification>
```bash
# Run all scraper tests to ensure nothing broken
npx vitest run tests/scraper/ --reporter=verbose

# Verify factory returns correct adapter counts per industry
npx vitest run tests/scraper/factory.test.ts --reporter=verbose

# Type check
npx tsc --noEmit
```
</verification>

<success_criteria>
- "violation" is a valid sourceType in the base adapter schema
- Three Socrata violation adapters (Austin, Dallas, Houston) scrape code violations with SODA3/SODA2 fallback
- Violation adapters are returned by factory for HVAC, roofing, and electrical industries only
- EIA utility rate adapter fetches state electricity prices with dedicated rate limiter
- Solar incentive lookup returns curated data for 15 states
- All new and existing scraper tests pass
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/18-intelligence-polish/18-01-SUMMARY.md`
</output>
