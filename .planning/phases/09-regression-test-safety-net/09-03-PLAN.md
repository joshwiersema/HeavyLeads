---
phase: 09-regression-test-safety-net
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - tests/regressions/sign-in-redirect.test.tsx
  - tests/regressions/mobile-nav.test.tsx
  - tests/regressions/landing-page.test.tsx
  - tests/regressions/pricing-display.test.tsx
  - tests/regressions/error-boundaries.test.tsx
  - tests/regressions/loading-states.test.tsx
autonomous: true
requirements:
  - TEST-01

must_haves:
  truths:
    - "Sign-in redirect test verifies error in org fetch shows error message (not infinite redirect)"
    - "Mobile nav test verifies drawer renders nav links and has open/close toggle"
    - "Landing page test verifies hero content with CTA links using <Link> (not nested <Link><Button>)"
    - "Pricing display test verifies monthlyPrice and setupFee render when provided, fallback when not"
    - "Error boundary tests verify 'Something went wrong' message and 'Try again' button render"
    - "Loading states test verifies Skeleton components render in loading.tsx files"
  artifacts:
    - path: "tests/regressions/sign-in-redirect.test.tsx"
      provides: "Regression test for bug fix #5"
      min_lines: 30
    - path: "tests/regressions/mobile-nav.test.tsx"
      provides: "Regression test for bug fix #8"
      min_lines: 25
    - path: "tests/regressions/landing-page.test.tsx"
      provides: "Regression test for bug fix #9"
      min_lines: 30
    - path: "tests/regressions/pricing-display.test.tsx"
      provides: "Regression test for bug fix #10"
      min_lines: 25
    - path: "tests/regressions/error-boundaries.test.tsx"
      provides: "Regression test for bug fix #11"
      min_lines: 30
    - path: "tests/regressions/loading-states.test.tsx"
      provides: "Regression test for bug fix #13"
      min_lines: 25
  key_links:
    - from: "tests/regressions/mobile-nav.test.tsx"
      to: "src/components/dashboard/mobile-nav.tsx"
      via: "import + @testing-library/react render"
      pattern: "render.*MobileNav"
    - from: "tests/regressions/error-boundaries.test.tsx"
      to: "src/app/error.tsx"
      via: "import + render"
      pattern: "Something went wrong"
    - from: "tests/regressions/landing-page.test.tsx"
      to: "src/app/page.tsx"
      via: "import client sub-components or test markup directly"
      pattern: "sign-up|sign-in"
    - from: "tests/regressions/pricing-display.test.tsx"
      to: "src/components/billing/plan-selector.tsx"
      via: "import + render with props"
      pattern: "PlanSelector"
---

<objective>
Write 6 component regression tests covering UI bug fixes in React components.

Purpose: These tests cover the UI-layer bugs: sign-in redirect loop fix, mobile nav drawer rendering, landing page for unauthenticated visitors (no nested Link/Button), pricing display with env var fallbacks, error boundaries, and loading skeleton states. Each test renders a React component with @testing-library/react and asserts on DOM content.

Output: 6 new test files in `tests/regressions/`, all passing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-regression-test-safety-net/09-RESEARCH.md
@.planning/phases/09-regression-test-safety-net/09-01-SUMMARY.md

@vitest.config.ts
@tests/setup.ts
@tests/helpers/auth.ts

@src/components/dashboard/mobile-nav.tsx
@src/components/billing/plan-selector.tsx
@src/app/error.tsx
@src/app/(dashboard)/error.tsx
@src/app/(dashboard)/dashboard/loading.tsx
@src/components/auth/sign-in-form.tsx
@src/app/page.tsx
</context>

<interfaces>
<!-- Key component interfaces the executor needs for rendering -->

From src/components/dashboard/mobile-nav.tsx:
```typescript
// Client component ("use client")
// Props: { userName: string } or similar
// Renders: Sheet/Drawer with navigation links (Leads, Bookmarks, Saved Searches, etc.)
// Bug fix: Was missing entirely, now renders properly with open/close toggle
```

From src/components/billing/plan-selector.tsx:
```typescript
// Client component
// Props include: monthlyPrice, setupFee (from env vars passed by parent)
// Bug fix: Shows actual price values when present, fallback text when missing
```

From src/app/error.tsx:
```typescript
// Client component ("use client")
// Props: { error: Error & { digest?: string }; reset: () => void }
// Renders: "Something went wrong" heading + "Try again" button calling reset()
```

From src/app/(dashboard)/error.tsx:
```typescript
// Client component ("use client")
// Same props pattern as root error.tsx
// Dashboard-specific error boundary
```

From src/app/(dashboard)/dashboard/loading.tsx:
```typescript
// Server component (default export)
// Renders: Skeleton placeholder components matching dashboard layout
```

From src/components/auth/sign-in-form.tsx:
```typescript
// Client component ("use client")
// Bug fix: wraps org fetch in try-catch; on error shows message instead of redirect loop
```

From src/app/page.tsx:
```typescript
// Landing page (server component but may have client sub-components)
// Bug fix: Uses <Link href="/sign-up"> (not <Link><Button> nesting), shows hero + CTA
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write regression tests for auth and navigation components (3 tests)</name>
  <files>
    tests/regressions/sign-in-redirect.test.tsx
    tests/regressions/mobile-nav.test.tsx
    tests/regressions/landing-page.test.tsx
  </files>
  <behavior>
    **sign-in-redirect.test.tsx** (bug fix #5):
    - Test: SignInForm renders email and password inputs plus a submit button
    - Test: SignInForm does NOT crash or redirect when org fetch fails (the try-catch fix)
    - Mock: vi.mock("@/lib/auth-client") to provide a signIn.email mock that resolves successfully, vi.mock("next/navigation") for useRouter. The key assertion is that the component renders without throwing, even when the underlying org fetch would fail.

    **mobile-nav.test.tsx** (bug fix #8):
    - Test: MobileNav renders navigation link text ("Leads", "Bookmarks", etc.)
    - Test: MobileNav renders a menu toggle button (hamburger/open trigger)
    - Test: Navigation links have correct href attributes (/dashboard, /dashboard/bookmarks, etc.)
    - Mock: vi.mock("next/link") with a simple <a> tag, vi.mock("next/navigation") for usePathname

    **landing-page.test.tsx** (bug fix #9):
    - Test: Landing page content contains sign-up and sign-in links as <a> tags (not nested <Link><Button>)
    - Test: Landing page renders hero/marketing content (heading, description)
    - Note: The page.tsx is a server component (async). Do NOT render it directly with @testing-library. Instead, test the static HTML output or test exported client sub-components. If there are no client sub-components, verify the page structure by importing and calling it as an async function, then render the result.
    - Mock: vi.mock("next/link") with simple <a> tag, possibly vi.mock("@/lib/auth") if it checks session
  </behavior>
  <action>
Create 3 component regression test files in `tests/regressions/`. Follow the established Pattern 2 (React Component Testing) from the research document.

**For all component tests:**
1. Mock `next/link` with: `vi.mock("next/link", () => ({ default: ({ children, href }: { children: React.ReactNode; href: string }) => <a href={href}>{children}</a> }))`
2. Mock `next/navigation` with: `vi.mock("next/navigation", () => ({ useRouter: () => ({ push: vi.fn(), replace: vi.fn(), refresh: vi.fn() }), usePathname: () => "/dashboard" }))`
3. Use `import { render, screen } from "@testing-library/react"` for component rendering
4. Use `@testing-library/jest-dom` matchers (`.toBeInTheDocument()`, `.toHaveAttribute()`)
5. `beforeEach(() => { vi.clearAllMocks(); })`

**sign-in-redirect.test.tsx specifics:**
- Mock `@/lib/auth-client` providing `authClient.signIn.email` as vi.fn()
- Render `<SignInForm />`
- Assert email input, password input, and submit button are present
- The test proves the component renders without crashing (the bug was a redirect loop / crash on org fetch failure). The try-catch ensures graceful handling.

**mobile-nav.test.tsx specifics:**
- Import `MobileNav` from `@/components/dashboard/mobile-nav`
- Read the actual component file first to determine exact props interface
- Mock any shadcn/ui Sheet components if needed (or they may render fine in jsdom)
- Assert navigation text and href values

**landing-page.test.tsx specifics:**
- The page is a server component (async function). Use one of these approaches:
  A. If it has client sub-components that contain the CTA links, test those directly
  B. If the page itself is simple, call it as `const result = await Page()` then `render(result)` (works for simple server components without headers/cookies)
  C. If approach B fails due to server imports, extract the testable assertion: search the file for `<Link` usage and write a structural test verifying no `<Link><Button>` nesting pattern exists (regex-based test on source code)
- Mock `@/lib/auth` and `next/headers` if the page checks session

CONSTRAINT: Do NOT modify any production source files. Only create test files.
  </action>
  <verify>
    <automated>cd /home/josh-wiersema/Documents/HeavyMachineryApp && npm run test -- tests/regressions/sign-in-redirect.test.tsx tests/regressions/mobile-nav.test.tsx tests/regressions/landing-page.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 component test files pass. SignInForm renders without crash, MobileNav shows nav links, landing page has proper CTA links.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write regression tests for billing UI, error boundaries, and loading states (3 tests)</name>
  <files>
    tests/regressions/pricing-display.test.tsx
    tests/regressions/error-boundaries.test.tsx
    tests/regressions/loading-states.test.tsx
  </files>
  <behavior>
    **pricing-display.test.tsx** (bug fix #10):
    - Test: PlanSelector displays monthlyPrice and setupFee values when passed as props
    - Test: PlanSelector displays fallback/default text when price props are missing or empty
    - Test: Price values are rendered as formatted currency strings

    **error-boundaries.test.tsx** (bug fix #11):
    - Test: Root error boundary (src/app/error.tsx) renders "Something went wrong" text
    - Test: Root error boundary renders a "Try again" button
    - Test: Dashboard error boundary (src/app/(dashboard)/error.tsx) renders similarly
    - Test: Clicking "Try again" calls the reset() function prop

    **loading-states.test.tsx** (bug fix #13):
    - Test: Dashboard loading component renders Skeleton placeholder elements
    - Test: Other loading.tsx files (if multiple exist) render Skeleton elements
    - Mock: May need to mock shadcn Skeleton component or verify it renders DOM elements with animation classes
  </behavior>
  <action>
Create 3 component regression test files in `tests/regressions/`.

**pricing-display.test.tsx specifics:**
- Read `src/components/billing/plan-selector.tsx` first to understand exact props interface
- Mock any dependencies (Stripe, auth-client, next/navigation as needed)
- Render `<PlanSelector />` with price props and assert values appear in DOM
- Render `<PlanSelector />` without price props and assert fallback text appears
- Use `screen.getByText()` or `screen.getAllByText()` for price value assertions

**error-boundaries.test.tsx specifics:**
- Import `ErrorBoundary` (or default export) from `@/app/error` and `@/app/(dashboard)/error`
- Create mock error: `new Error("Test error")`
- Create mock reset: `vi.fn()`
- Render with props: `<ErrorComponent error={mockError} reset={mockReset} />`
- Assert: `screen.getByText(/something went wrong/i)` is present
- Assert: `screen.getByRole("button", { name: /try again/i })` is present
- Simulate click on "Try again" button and assert `mockReset` was called

**loading-states.test.tsx specifics:**
- Import loading components from dashboard loading.tsx files
- The loading components are server components but typically just render JSX without async/headers
- Render and assert that Skeleton-like elements (divs with animate-pulse class or similar) are present
- Use `container.querySelectorAll('[class*="animate"]')` or similar for skeleton detection
- If shadcn Skeleton uses specific class patterns, assert those

For all files: Mock `next/link` and `next/navigation` as needed. Use `beforeEach(() => { vi.clearAllMocks(); })`.

CONSTRAINT: Do NOT modify any production source files. Only create test files.
  </action>
  <verify>
    <automated>cd /home/josh-wiersema/Documents/HeavyMachineryApp && npm run test -- tests/regressions/pricing-display.test.tsx tests/regressions/error-boundaries.test.tsx tests/regressions/loading-states.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 component test files pass. Pricing shows values and fallbacks, error boundaries render message and reset button, loading states render skeletons.</done>
</task>

</tasks>

<verification>
Run all regression tests together:
```bash
npm run test -- tests/regressions/ --reporter=verbose
```
Expected: All 15 test files pass (9 from Plan 02 + 6 from this Plan 03), 0 failures. Then run full suite:
```bash
npm run test
```
Expected: All tests pass (285+ existing + ~30-50 new regression test cases).
</verification>

<success_criteria>
- 6 new component test files exist in tests/regressions/
- All 6 component test files pass
- Each test targets a specific UI bug fix and would fail if the fix were reverted
- @testing-library/react render + screen pattern used consistently
- next/link and next/navigation mocked per established patterns
- Existing test suite still green (no regressions)
- Zero production source files modified
- Combined with Plan 02: all 15 bug fixes now have regression test coverage
</success_criteria>

<output>
After completion, create `.planning/phases/09-regression-test-safety-net/09-03-SUMMARY.md`
</output>
