---
phase: 11-forgot-password
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/validators/auth.ts
  - src/components/emails/password-reset.tsx
  - src/lib/auth.ts
  - src/components/auth/forgot-password-form.tsx
  - src/app/(auth)/forgot-password/page.tsx
  - src/components/auth/sign-in-form.tsx
  - tests/auth/send-reset-password.test.ts
  - tests/auth/forgot-password-form.test.tsx
  - tests/auth/sign-in-forgot-link.test.tsx
autonomous: true
requirements:
  - AUTH-01

must_haves:
  truths:
    - "User can click 'Forgot password?' on the sign-in page and navigate to /forgot-password"
    - "User can enter their email on the forgot-password page and see a success message (regardless of whether email exists)"
    - "System sends a password reset email via Resend when a registered email requests a reset"
  artifacts:
    - path: "src/lib/validators/auth.ts"
      provides: "forgotPasswordSchema and resetPasswordSchema Zod schemas"
      contains: "forgotPasswordSchema"
    - path: "src/components/emails/password-reset.tsx"
      provides: "React Email template for password reset"
      exports: ["PasswordResetEmail"]
    - path: "src/lib/auth.ts"
      provides: "sendResetPassword callback in emailAndPassword config"
      contains: "sendResetPassword"
    - path: "src/components/auth/forgot-password-form.tsx"
      provides: "Forgot password form component"
      exports: ["ForgotPasswordForm"]
    - path: "src/app/(auth)/forgot-password/page.tsx"
      provides: "Forgot password page route"
    - path: "src/components/auth/sign-in-form.tsx"
      provides: "Updated sign-in form with forgot password link"
      contains: "forgot-password"
  key_links:
    - from: "src/components/auth/sign-in-form.tsx"
      to: "/forgot-password"
      via: "Next.js Link component"
      pattern: "href.*forgot-password"
    - from: "src/components/auth/forgot-password-form.tsx"
      to: "authClient.requestPasswordReset"
      via: "form onSubmit handler"
      pattern: "requestPasswordReset"
    - from: "src/lib/auth.ts"
      to: "Resend API"
      via: "sendResetPassword callback"
      pattern: "resend\\.emails\\.send"
    - from: "src/lib/auth.ts"
      to: "src/components/emails/password-reset.tsx"
      via: "PasswordResetEmail import for react prop"
      pattern: "PasswordResetEmail"
---

<objective>
Implement the forgot-password request flow: server-side auth config, email template, validation schemas, forgot-password form/page, and sign-in page link.

Purpose: Enable locked-out users to initiate password recovery by entering their email on a dedicated page accessible from sign-in.
Output: Working forgot-password request flow (user enters email -> system sends reset email via Resend) plus all validation schemas needed by both plans.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-forgot-password/11-RESEARCH.md

@src/lib/auth.ts
@src/lib/auth-client.ts
@src/lib/validators/auth.ts
@src/lib/email/send-digest.ts
@src/components/emails/daily-digest.tsx
@src/components/auth/sign-in-form.tsx
@src/components/settings/change-password-form.tsx
@src/app/(auth)/layout.tsx
@src/app/(auth)/sign-in/page.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/validators/auth.ts (EXISTING - will be extended):
```typescript
export const signUpSchema = z.object({ ... });
export type SignUpFormData = z.infer<typeof signUpSchema>;
export const signInSchema = z.object({ ... });
export type SignInFormData = z.infer<typeof signInSchema>;
```

From src/lib/auth-client.ts (EXISTING - no changes needed):
```typescript
export const authClient = createAuthClient({
  plugins: [organizationClient(), stripeClient({ subscription: true })],
});
// Available methods (already built-in, no changes needed):
// authClient.requestPasswordReset({ email, redirectTo }) -> Promise<{ data, error }>
// authClient.resetPassword({ newPassword, token }) -> Promise<{ data, error }>
```

From src/lib/auth.ts (EXISTING - emailAndPassword section to modify):
```typescript
emailAndPassword: { enabled: true },
// Needs: sendResetPassword callback, resetPasswordTokenExpiresIn, revokeSessionsOnPasswordReset
```

From src/components/emails/daily-digest.tsx (EXISTING - style reference):
```typescript
// Inline style constants to reuse for brand consistency:
// bodyStyle, containerStyle, headerStyle, brandStyle, headingStyle, buttonStyle, etc.
```

From src/app/(auth)/layout.tsx (EXISTING - provides centered layout for auth pages):
```typescript
// Wraps children in: flex min-h-screen items-center justify-center bg-muted/40 p-4
// with max-w-md container
```

From src/components/settings/change-password-form.tsx (EXISTING - pattern reference):
```typescript
// Uses: react-hook-form + zodResolver + z.refine for password confirmation
// Uses: authClient.changePassword(), toast from sonner, Button/Input/Label/Card UI
// Uses: Loader2 spinner from lucide-react during submission
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validation schemas, email template, and auth.ts sendResetPassword config</name>
  <files>
    src/lib/validators/auth.ts,
    src/components/emails/password-reset.tsx,
    src/lib/auth.ts,
    tests/auth/send-reset-password.test.ts
  </files>
  <behavior>
    - Test: forgotPasswordSchema accepts valid email, rejects empty/invalid email
    - Test: resetPasswordSchema accepts matching passwords >= 8 chars, rejects mismatched passwords, rejects passwords < 8 chars
    - Test: PasswordResetEmail renders with url and userName props (renders "Reset Your Password" heading, reset link, expiry notice)
    - Test: sendResetPassword config in auth.ts calls Resend with correct from/to/subject/react args when RESEND_API_KEY is set
    - Test: sendResetPassword logs error and throws when RESEND_API_KEY is missing (password reset is critical path, unlike digest which silently skips)
  </behavior>
  <action>
    1. **Add schemas to src/lib/validators/auth.ts** (append, do not modify existing schemas):
       - `forgotPasswordSchema`: z.object with email field (z.string().email("Please enter a valid email address"))
       - `resetPasswordSchema`: z.object with newPassword (z.string().min(8, "Password must be at least 8 characters")) and confirmPassword (z.string().min(1, "Please confirm your new password")), plus z.refine for password match (same pattern as change-password-form.tsx)
       - Export both schemas and their inferred types

    2. **Create src/components/emails/password-reset.tsx**:
       - Follow the exact inline-style pattern from daily-digest.tsx for brand consistency (same bodyStyle, containerStyle, headerStyle, brandStyle, headingStyle values)
       - Props: `{ url: string; userName: string }`
       - Content: HeavyLeads brand header, "Reset Your Password" heading, greeting with userName, instruction text mentioning 1-hour expiry, "Reset Password" button linking to url, "If you didn't request this" disclaimer
       - Export as named export `PasswordResetEmail`

    3. **Modify src/lib/auth.ts** emailAndPassword config:
       - Change `emailAndPassword: { enabled: true }` to expanded object with:
         - `enabled: true`
         - `sendResetPassword: async ({ user, url }) => { ... }` callback
         - `resetPasswordTokenExpiresIn: 3600` (1 hour)
         - `revokeSessionsOnPasswordReset: true`
       - Inside sendResetPassword callback:
         - Import PasswordResetEmail at top of file (safe -- React component, no side effects)
         - Instantiate Resend client INSIDE the callback (not at module top level -- per project decision "Never add side-effect imports to db/index.ts or auth.ts")
         - Use `(process.env.RESEND_API_KEY ?? "").trim()` per env var convention
         - If RESEND_API_KEY is empty after trim, `console.error("[auth] RESEND_API_KEY not set, cannot send password reset email")` and `throw new Error("Email service not configured")` (password reset is critical path -- unlike digest, must NOT silently skip)
         - Use `(process.env.RESEND_FROM_EMAIL ?? "").trim() || "HeavyLeads <onboarding@resend.dev>"` for from address
         - Call `resend.emails.send({ from, to: user.email, subject: "Reset your HeavyLeads password", react: PasswordResetEmail({ url, userName: user.name }) })`
         - Log success: `console.log("[auth] Password reset email sent to", user.email)`

    4. **Create tests/auth/send-reset-password.test.ts**:
       - Test the Zod schemas (forgotPasswordSchema, resetPasswordSchema) with valid and invalid inputs
       - Test PasswordResetEmail renders correctly (import and call as function, check return structure)
       - For the auth.ts sendResetPassword integration: since it's embedded in the better-auth config, test by importing the auth config and verifying the sendResetPassword function exists and is callable. Mock Resend to verify it gets called with the right args. Use vi.mock for resend module.
  </action>
  <verify>
    <automated>npx vitest run tests/auth/send-reset-password.test.ts -x</automated>
  </verify>
  <done>
    - forgotPasswordSchema and resetPasswordSchema exported from validators/auth.ts
    - PasswordResetEmail component renders branded email with reset link
    - auth.ts emailAndPassword config includes sendResetPassword callback that calls Resend
    - Missing RESEND_API_KEY throws error (critical path)
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Forgot password form, page route, and sign-in link</name>
  <files>
    src/components/auth/forgot-password-form.tsx,
    src/app/(auth)/forgot-password/page.tsx,
    src/components/auth/sign-in-form.tsx,
    tests/auth/forgot-password-form.test.tsx,
    tests/auth/sign-in-forgot-link.test.tsx
  </files>
  <behavior>
    - Test: ForgotPasswordForm renders email input and submit button
    - Test: ForgotPasswordForm shows validation error for empty/invalid email
    - Test: ForgotPasswordForm shows generic success message after submission (prevents user enumeration -- always says "If an account exists...")
    - Test: ForgotPasswordForm disables submit button during loading
    - Test: SignInForm contains a "Forgot password?" link with href="/forgot-password"
  </behavior>
  <action>
    1. **Create src/components/auth/forgot-password-form.tsx**:
       - Follow the same pattern as sign-in-form.tsx: "use client", react-hook-form + zodResolver, useState for loading/submitted states
       - Import forgotPasswordSchema and ForgotPasswordFormData from @/lib/validators/auth
       - Import authClient from @/lib/auth-client
       - Form flow:
         - User enters email, clicks "Send Reset Link"
         - Call `authClient.requestPasswordReset({ email: data.email, redirectTo: "/reset-password" })`
         - On completion (success OR error), show generic message: "If an account exists with that email, we've sent a password reset link. Check your inbox." (prevents user enumeration)
         - Show a "Back to sign in" link below the success message
       - Use Card/CardHeader/CardTitle/CardDescription/CardContent/CardFooter from @/components/ui/card
       - Use Button/Input/Label from @/components/ui
       - Use Loader2 spinner from lucide-react during submission (same pattern as change-password-form.tsx)
       - CardTitle: "Forgot your password?"
       - CardDescription: "Enter your email and we'll send you a reset link"
       - CardFooter: Link back to /sign-in ("Remember your password? Sign in")

    2. **Create src/app/(auth)/forgot-password/page.tsx**:
       - Server component page (same pattern as sign-in/page.tsx)
       - Export metadata: `{ title: "Forgot Password | HeavyLeads" }`
       - Render `<ForgotPasswordForm />`

    3. **Modify src/components/auth/sign-in-form.tsx**:
       - Add a "Forgot password?" link between the password input field and the submit button
       - Use Next.js Link component (already imported)
       - Markup: `<div className="flex justify-end"><Link href="/forgot-password" className="text-sm text-primary hover:underline">Forgot password?</Link></div>`
       - Place it after the password field's error message div, before the Button

    4. **Create tests/auth/forgot-password-form.test.tsx**:
       - Mock @/lib/auth-client (vi.mock) to provide authClient.requestPasswordReset as vi.fn() returning { data: { status: true }, error: null }
       - Mock next/navigation useRouter
       - Test form renders email input and submit button
       - Test validation error appears for invalid email
       - Test success message appears after valid submission
       - Test submit button shows loading state during submission

    5. **Create tests/auth/sign-in-forgot-link.test.tsx**:
       - Mock @/lib/auth-client, next/navigation
       - Render SignInForm
       - Assert "Forgot password?" link exists with href="/forgot-password"
  </action>
  <verify>
    <automated>npx vitest run tests/auth/forgot-password-form.test.tsx tests/auth/sign-in-forgot-link.test.tsx -x</automated>
  </verify>
  <done>
    - ForgotPasswordForm renders with email input, validates, calls requestPasswordReset, shows generic success message
    - /forgot-password page route renders the form within the (auth) layout
    - Sign-in page has "Forgot password?" link navigating to /forgot-password
    - All tests pass
  </done>
</task>

</tasks>

<verification>
npx vitest run tests/auth/ -x
</verification>

<success_criteria>
- User can navigate from sign-in page to /forgot-password via "Forgot password?" link
- Forgot password page renders a form accepting email input
- Submitting a valid email calls better-auth's requestPasswordReset and shows a generic success message (no user enumeration)
- Server-side auth.ts is configured with sendResetPassword that sends branded email via Resend
- All 3 test files pass (send-reset-password, forgot-password-form, sign-in-forgot-link)
</success_criteria>

<output>
After completion, create `.planning/phases/11-forgot-password/11-01-SUMMARY.md`
</output>
