---
phase: 06-billing-and-launch-readiness
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/app/(dashboard)/settings/billing/page.tsx
  - src/app/(dashboard)/settings/layout.tsx
  - src/components/billing/subscribe-button.tsx
  - src/components/billing/manage-billing-button.tsx
  - src/components/billing/billing-status.tsx
  - tests/billing/access-gate.test.ts
  - tests/billing/billing-page.test.tsx
  - tests/helpers/billing.ts
autonomous: true
requirements: [PLAT-05]

must_haves:
  truths:
    - "Billing page shows a subscribe button when organization has no active subscription"
    - "Billing page shows subscription status and manage button when organization has an active subscription"
    - "Subscribe button redirects to Stripe Checkout with both recurring and one-time setup fee line items"
    - "Manage button redirects to Stripe Customer Portal for payment method and cancellation management"
    - "Settings navigation includes a Billing link alongside Account and Company"
    - "Access gate redirects to billing page when no subscription exists"
  artifacts:
    - path: "src/app/(dashboard)/settings/billing/page.tsx"
      provides: "Billing settings page with subscribe or manage view"
      min_lines: 30
    - path: "src/components/billing/subscribe-button.tsx"
      provides: "Client component that calls authClient.subscription.upgrade"
      contains: "subscription.upgrade"
    - path: "src/components/billing/manage-billing-button.tsx"
      provides: "Client component that calls authClient.subscription.billingPortal"
      contains: "subscription.billingPortal"
    - path: "src/components/billing/billing-status.tsx"
      provides: "Component showing current plan name, status, and renewal date"
    - path: "src/app/(dashboard)/settings/layout.tsx"
      provides: "Settings nav with Billing link added"
      contains: "/settings/billing"
    - path: "tests/billing/access-gate.test.ts"
      provides: "Tests for getActiveSubscription helper"
    - path: "tests/billing/billing-page.test.tsx"
      provides: "Tests for billing page rendering with/without subscription"
  key_links:
    - from: "src/components/billing/subscribe-button.tsx"
      to: "src/lib/auth-client.ts"
      via: "authClient.subscription.upgrade()"
      pattern: "authClient\\.subscription\\.upgrade"
    - from: "src/components/billing/manage-billing-button.tsx"
      to: "src/lib/auth-client.ts"
      via: "authClient.subscription.billingPortal()"
      pattern: "authClient\\.subscription\\.billingPortal"
    - from: "src/app/(dashboard)/settings/billing/page.tsx"
      to: "src/lib/billing.ts"
      via: "getActiveSubscription for conditional rendering"
      pattern: "getActiveSubscription"
---

<objective>
Build the billing settings page with subscribe/manage UI and add tests for the access gate and billing components.

Purpose: This completes PLAT-05 by giving company admins a self-service billing interface. Without this page, users redirected by the access gate would see a 404.
Output: Working billing page at /settings/billing with subscribe and manage flows, plus test coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-billing-and-launch-readiness/06-RESEARCH.md
@.planning/phases/06-billing-and-launch-readiness/06-01-SUMMARY.md

<interfaces>
<!-- Contracts created by Plan 01 that this plan depends on -->

From src/lib/stripe.ts (created in Plan 01):
```typescript
import Stripe from "stripe";

export const stripeClient: Stripe;

export const PRICES: {
  monthlySubscription: string;
  setupFee: string;
};
```

From src/lib/billing.ts (created in Plan 01):
```typescript
export async function getActiveSubscription(organizationId: string): Promise<SubscriptionRow | null>;
```

From src/lib/auth-client.ts (modified in Plan 01):
```typescript
"use client";
import { createAuthClient } from "better-auth/react";
import { organizationClient } from "better-auth/client/plugins";
import { stripeClient as stripeClientPlugin } from "@better-auth/stripe/client";

export const authClient = createAuthClient({
  plugins: [
    organizationClient(),
    stripeClientPlugin({ subscription: true }),
  ],
});

// authClient.subscription.upgrade({ plan, successUrl, cancelUrl, referenceId, customerType }) -> { data: { url } }
// authClient.subscription.billingPortal({ returnUrl }) -> { data: { url } }

export const { useSession, signIn, signUp, signOut, useActiveOrganization } = authClient;
```

From src/app/(dashboard)/settings/layout.tsx (existing):
```typescript
// Settings nav with Account and Company links
// Billing link needs to be added here
```

From src/app/(dashboard)/layout.tsx (modified in Plan 01):
```typescript
// Guard chain: auth -> org -> onboarding -> subscription -> render
// Subscription check uses getActiveSubscription(activeOrganizationId)
// Redirects to /settings/billing when no active subscription
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build billing settings page with subscribe and manage flows</name>
  <files>
    src/app/(dashboard)/settings/billing/page.tsx,
    src/app/(dashboard)/settings/layout.tsx,
    src/components/billing/subscribe-button.tsx,
    src/components/billing/manage-billing-button.tsx,
    src/components/billing/billing-status.tsx
  </files>
  <action>
    1. Create `src/components/billing/subscribe-button.tsx` ("use client"):
       - Accept props: `organizationId: string`
       - Import `authClient` from "@/lib/auth-client"
       - On click, call `authClient.subscription.upgrade({ plan: "standard", successUrl: "/dashboard", cancelUrl: "/settings/billing", referenceId: organizationId, customerType: "organization" })`
       - If data.url returned, `window.location.href = data.url` to redirect to Stripe Checkout
       - Show loading state (disabled button with "Redirecting..." text) while request is in flight
       - Use shadcn Button component. Label: "Subscribe Now"
       - Handle error with toast notification via sonner

    2. Create `src/components/billing/manage-billing-button.tsx` ("use client"):
       - Import `authClient` from "@/lib/auth-client"
       - On click, call `authClient.subscription.billingPortal({ returnUrl: "/settings/billing" })`
       - If data.url returned, `window.location.href = data.url` to redirect to Stripe Customer Portal
       - Show loading state while request is in flight
       - Use shadcn Button with variant="outline". Label: "Manage Billing"
       - Handle error with toast notification via sonner

    3. Create `src/components/billing/billing-status.tsx` (server component, no "use client"):
       - Accept props: `subscription: { plan: string; status: string; periodEnd: Date | null; cancelAtPeriodEnd: boolean }`
       - Display: Plan name ("HeavyLeads Standard"), Status badge (Active = green, Trialing = blue, Past Due = amber, Canceled = red), Next renewal date (formatted from periodEnd), Cancellation notice if cancelAtPeriodEnd is true
       - Use shadcn Card component with clean layout
       - If status is "past_due", show a warning message: "Your payment failed. Please update your payment method."

    4. Create `src/app/(dashboard)/settings/billing/page.tsx`:
       - This is a SERVER component
       - Import auth from "@/lib/auth", headers from "next/headers", getActiveSubscription from "@/lib/billing"
       - Get session, get activeOrganizationId
       - Call getActiveSubscription(activeOrganizationId)
       - If NO active subscription: render a "Get Started" card with pricing info ($X/mo + one-time setup fee), feature bullet points (Daily lead feed, Multi-source intelligence, Equipment matching, Email digests), and the SubscribeButton component passing organizationId
       - If active subscription: render BillingStatus component with subscription data, and ManageBillingButton below it
       - Page title: "Billing" with subtitle "Manage your subscription and payment details"

    5. Modify `src/app/(dashboard)/settings/layout.tsx`:
       - Add a "Billing" nav link pointing to "/settings/billing" after the "Company" link
       - Import CreditCard icon from lucide-react for visual consistency (Account and Company don't have icons currently, so just add the text link without icon to match existing pattern)

    IMPORTANT: The billing page must be accessible even when the dashboard access gate would normally redirect. Since /settings/billing is INSIDE the (dashboard) route group, the access gate in layout.tsx must exempt the billing path. Update layout.tsx to check if the current path is /settings/billing and skip the subscription redirect if so. Use headers() to read the x-next-url or x-pathname header, OR pass the pathname via a different mechanism. The simplest approach: in layout.tsx, check the current URL path and skip the subscription gate for /settings/billing. Use `new URL(request.headers.get("x-url") || "")` or check headers for the path. Alternatively, move the billing page OUTSIDE the dashboard route group. The SIMPLEST solution: check the pathname from the incoming request headers -- Next.js App Router makes the full URL available via headers(). Check `(await headers()).get("x-invoke-path")` or parse the referer. If headers don't reliably provide the path, the fallback approach is to NOT redirect to billing from the layout and instead show a banner/modal on the dashboard itself. Research the headers approach first; if unreliable, use the banner approach.

    ACTUALLY, the cleanest solution per Next.js 16 conventions: Move the billing page to a SEPARATE route group that shares the settings layout but is NOT under the (dashboard) layout. However, this adds complexity. The PRAGMATIC solution: in the dashboard layout.tsx, after checking subscription status, if no subscription, redirect to "/subscribe" -- a NEW page outside the (dashboard) group. BUT this breaks the settings navigation pattern.

    REVISED APPROACH (simplest, most reliable): In the dashboard layout.tsx subscription check, do NOT redirect. Instead, pass `hasSubscription: boolean` as a prop or use a different pattern. Actually, the CLEANEST approach is: the billing page should work despite the redirect because the redirect in layout.tsx can check pathname. In Next.js App Router, the layout can detect the current route segment. Use the `children` props approach -- no, that doesn't help.

    FINAL APPROACH: The access gate in Plan 01's layout.tsx should use a pathname check. When adding the subscription gate, also check if the current path starts with "/settings/billing" by reading `(await headers()).get("x-nextjs-data")` or using `new URL((await headers()).get("referer") || "").pathname`. If that proves unreliable, use Next.js `useSelectedLayoutSegments` -- but that's client-only. The MOST RELIABLE server-side approach in Next.js App Router: pass the path via a middleware that sets a custom header containing the pathname. But adding middleware is scope creep.

    SIMPLEST RELIABLE APPROACH: Modify the Plan 01 access gate in layout.tsx to use `headers().get("x-invoke-path")` OR simply accept that the billing page will be inside the gated layout and handle the circular redirect by adding a query parameter `?setup=billing` to the redirect URL, then check for this param in the layout to break the loop. Example:
    - layout.tsx: `redirect("/settings/billing?gate=subscription")`
    - In layout.tsx: if searchParams includes gate=subscription, skip the subscription check

    But layouts don't receive searchParams in Next.js App Router.

    ACTUAL SIMPLEST: Create the billing/subscribe page OUTSIDE the (dashboard) group entirely. Create `src/app/(billing)/settings/billing/page.tsx` in its own route group with a minimal layout that just checks auth (not subscription). This is the cleanest separation.

    REVISED FINAL: Create `src/app/(billing)/layout.tsx` -- a lightweight layout that checks auth and org but NOT subscription. Place the billing page at `src/app/(billing)/settings/billing/page.tsx`. The dashboard layout redirect points to `/settings/billing` which resolves to the (billing) group page, not the (dashboard) group. This requires NO changes to the dashboard layout from Plan 01. The settings sub-navigation (Account, Company, Billing) can still render within this layout. Import the settings nav as a shared component.

    TO KEEP IT SIMPLE: Just create the billing page at `src/app/(auth)/billing/page.tsx` (reusing the existing non-dashboard auth layout) OR create a new standalone route. The settings nav won't appear but that's fine -- the billing page is a standalone flow for subscribing.

    WAIT -- stepping back. The simplest approach that preserves UX:
    1. The dashboard layout redirects non-subscribers to `/settings/billing`
    2. `/settings/billing` is INSIDE the dashboard layout
    3. This creates a redirect loop

    The fix: In the layout.tsx subscription check, read the current request URL. In Next.js App Router, you CAN use the `headers()` API -- specifically, `x-invoke-path` or `x-middleware-request-x-pathname` headers are set by Next.js internally. If those aren't available, the `next-url` header is available. Test and use whichever works.

    If no header approach works reliably, fall back to: Move the billing page to a new `(billing)` route group with its own layout. The billing page URL becomes `/billing` instead of `/settings/billing`. Update the Plan 01 redirect to point to `/billing`.

    EXECUTOR INSTRUCTIONS:
    - FIRST try: In dashboard layout.tsx, read pathname from headers. Try `(await headers()).get("x-invoke-path")`, `(await headers()).get("next-url")`, or `(await headers()).get("x-nextjs-data")`. If any returns the current path, use it to skip the subscription gate when path starts with "/settings/billing".
    - IF headers approach fails: Create the billing page at `/billing` (outside dashboard group) with a minimal auth-only layout at `src/app/(billing)/layout.tsx`. Update Plan 01's redirect target from "/settings/billing" to "/billing". Add a "Billing" link in the settings nav that points to "/billing".
    - The billing page itself is the same regardless of which route it lives in.

    NOTE ON SETTINGS NAV: If billing ends up outside the dashboard group, the settings layout nav still gets a "Billing" link but it points to "/billing" instead of "/settings/billing". This is acceptable.
  </action>
  <verify>
    <automated>cd /home/josh-wiersema/Documents/HeavyMachineryApp && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Billing page renders at its route (either /settings/billing or /billing)
    - Non-subscribers see subscribe card with pricing and SubscribeButton
    - Subscribers see BillingStatus + ManageBillingButton
    - SubscribeButton calls authClient.subscription.upgrade with correct params
    - ManageBillingButton calls authClient.subscription.billingPortal
    - Settings nav includes Billing link
    - No redirect loop between dashboard gate and billing page
    - TypeScript compiles cleanly
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add test coverage for access gate and billing components</name>
  <files>
    tests/billing/access-gate.test.ts,
    tests/billing/billing-page.test.tsx,
    tests/helpers/billing.ts
  </files>
  <behavior>
    - Test 1: getActiveSubscription returns subscription when status is "active"
    - Test 2: getActiveSubscription returns subscription when status is "trialing"
    - Test 3: getActiveSubscription returns null when status is "canceled"
    - Test 4: getActiveSubscription returns null when status is "past_due"
    - Test 5: getActiveSubscription returns null when no subscription exists for org
    - Test 6: Billing page renders subscribe button when no subscription (component test)
    - Test 7: Billing page renders subscription status when subscription exists (component test)
    - Test 8: Billing page shows correct status badge color per status type
  </behavior>
  <action>
    1. Create `tests/helpers/billing.ts`:
       - Export mock subscription factory: `createMockSubscription(overrides)` that returns a subscription row with defaults (id, plan: "standard", referenceId: "org-123", status: "active", periodEnd: 30 days from now, cancelAtPeriodEnd: false, etc.)
       - Export mock Stripe event factories if needed for future webhook tests

    2. Create `tests/billing/access-gate.test.ts`:
       - Mock the database module (`@/lib/db`) to control query results
       - Test getActiveSubscription with various subscription statuses:
         - "active" -> returns subscription
         - "trialing" -> returns subscription
         - "canceled" -> returns null
         - "past_due" -> returns null
         - No rows -> returns null
       - Use vi.mock for db module, return controlled values from db.query.subscription.findFirst

    3. Create `tests/billing/billing-page.test.tsx`:
       - Test the BillingStatus component renders correct status badge and renewal date
       - Test the SubscribeButton component renders and is clickable (mock authClient)
       - Test the ManageBillingButton component renders and is clickable (mock authClient)
       - Use @testing-library/react for component rendering
       - Mock authClient.subscription.upgrade and .billingPortal to return { data: { url: "https://checkout.stripe.com/test" } }

    Run tests with: `npx vitest run tests/billing/ --reporter=verbose`
  </action>
  <verify>
    <automated>cd /home/josh-wiersema/Documents/HeavyMachineryApp && npx vitest run tests/billing/ --reporter=verbose</automated>
  </verify>
  <done>
    - All access gate tests pass (5 scenarios covering active, trialing, canceled, past_due, missing)
    - All billing component tests pass (status display, subscribe button, manage button)
    - Tests run in under 10 seconds
    - No test relies on actual Stripe API calls
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit` passes
2. Tests pass: `npx vitest run tests/billing/ --reporter=verbose` all green
3. Full test suite: `npx vitest run` no regressions
4. Billing page accessible: No redirect loop when non-subscriber visits billing page
5. Subscribe flow: SubscribeButton triggers Stripe Checkout redirect
6. Manage flow: ManageBillingButton triggers Customer Portal redirect
</verification>

<success_criteria>
- Billing page renders correctly for both subscribers and non-subscribers
- Subscribe button initiates Stripe Checkout with correct plan and setup fee
- Manage button opens Stripe Customer Portal
- Settings navigation includes Billing link
- Access gate test suite covers all subscription status scenarios
- Component tests verify correct rendering and button behavior
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/06-billing-and-launch-readiness/06-02-SUMMARY.md`
</output>
