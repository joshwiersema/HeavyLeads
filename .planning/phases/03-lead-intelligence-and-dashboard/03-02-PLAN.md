---
phase: 03-lead-intelligence-and-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/(dashboard)/dashboard/lead-card.tsx
  - src/app/(dashboard)/dashboard/lead-filters.tsx
  - src/app/(dashboard)/dashboard/lead-card-skeleton.tsx
  - src/app/(dashboard)/layout.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/select.tsx
  - src/components/ui/slider.tsx
  - src/components/ui/skeleton.tsx
autonomous: true
requirements: [UX-01, LEAD-05, LEAD-06, UX-05]

user_setup:
  - service: shadcn-ui
    why: "Adding badge, select, slider, skeleton components"
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "Dashboard shows a daily lead feed with cards sorted by relevance score (descending) with recency as tiebreaker"
    - "Each lead card displays project type, address, freshness badge (New/This Week/Older), relevance score, inferred equipment types, and distance from HQ"
    - "User can filter leads by equipment type via multi-select (defaults to show-all)"
    - "User can filter leads by geographic radius via slider (10-500 miles, defaults to company service radius)"
    - "Filter state persists in URL search params for bookmarkability and server-side rendering"
    - "Empty state shows friendly message with suggestions to adjust filters when no leads match"
    - "Loading state shows skeleton cards while data loads"
  artifacts:
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Lead feed server component replacing placeholder"
      min_lines: 40
    - path: "src/app/(dashboard)/dashboard/lead-card.tsx"
      provides: "Individual lead card with freshness badge, score, equipment tags, distance"
      min_lines: 30
    - path: "src/app/(dashboard)/dashboard/lead-filters.tsx"
      provides: "Client component with equipment multi-select and radius slider"
      min_lines: 40
    - path: "src/app/(dashboard)/dashboard/lead-card-skeleton.tsx"
      provides: "Loading skeleton for Suspense boundary"
      min_lines: 10
  key_links:
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "src/lib/leads/queries.ts"
      via: "calls getFilteredLeads with session org and company profile"
      pattern: "getFilteredLeads"
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "searchParams"
      via: "reads equipment and radius filters from URL"
      pattern: "await searchParams"
    - from: "src/app/(dashboard)/dashboard/lead-filters.tsx"
      to: "URL searchParams"
      via: "updates URL on filter change using router.replace"
      pattern: "router\\.replace"
    - from: "src/app/(dashboard)/dashboard/lead-card.tsx"
      to: "src/lib/leads/types.ts"
      via: "uses EnrichedLead type for props"
      pattern: "EnrichedLead"
---

<objective>
Build the daily lead feed dashboard: replace the placeholder dashboard with a filterable, scored lead feed using server components and URL-based filter state.

Purpose: This is the core user-facing feature -- what sales reps see every morning. Cards show scored leads with freshness badges, equipment type filters narrow results, and the radius slider controls geographic scope.

Output: Dashboard page with lead cards, filter controls, loading skeletons, and updated sidebar navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lead-intelligence-and-dashboard/03-RESEARCH.md
@.planning/phases/03-lead-intelligence-and-dashboard/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (src/lib/leads/) -->

From src/lib/leads/types.ts (created in Plan 01):
```typescript
export type FreshnessBadge = "New" | "This Week" | "Older";
export interface InferredEquipment {
  type: EquipmentType;
  confidence: "high" | "medium" | "low";
  reason: string;
}
export interface TimelineWindow {
  phase: string;
  equipment: EquipmentType[];
  urgency: "Now" | "Soon" | "Later";
  description: string;
}
export interface EnrichedLead {
  // All leads table columns plus:
  distance: number | null;
  inferredEquipment: InferredEquipment[];
  score: number;
  freshness: FreshnessBadge;
  timeline: TimelineWindow[];
}
export function getFreshnessBadge(scrapedAt: Date): FreshnessBadge;
```

From src/lib/leads/queries.ts (created in Plan 01):
```typescript
export async function getFilteredLeads(params: {
  hqLat: number;
  hqLng: number;
  serviceRadiusMiles: number;
  dealerEquipment: string[];
  radiusMiles?: number;
  equipmentFilter?: string[];
  limit?: number;
  offset?: number;
}): Promise<EnrichedLead[]>;
```

From src/types/index.ts:
```typescript
export const EQUIPMENT_TYPES = [...] as const;
export type EquipmentType = (typeof EQUIPMENT_TYPES)[number];
```

From src/lib/db/schema/company-profiles.ts:
```typescript
// companyProfiles.equipmentTypes: text("equipment_types").array().default([])
// companyProfiles.hqLat: real("hq_lat")
// companyProfiles.hqLng: real("hq_lng")
// companyProfiles.serviceRadiusMiles: real("service_radius_miles").default(50)
```

Established patterns:
- Session: `auth.api.getSession({ headers: await headers() })`
- Tenant query: `db.query.companyProfiles.findFirst({ where: eq(companyProfiles.organizationId, session.session.activeOrganizationId) })`
- Next.js 16 searchParams: `searchParams: Promise<{ [key: string]: string | string[] | undefined }>` -- must await
- Client filter updates: `router.replace(\`\${pathname}?\${params.toString()}\`)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shadcn UI components and build lead card + skeleton</name>
  <files>
    src/components/ui/badge.tsx,
    src/components/ui/select.tsx,
    src/components/ui/slider.tsx,
    src/components/ui/skeleton.tsx,
    src/app/(dashboard)/dashboard/lead-card.tsx,
    src/app/(dashboard)/dashboard/lead-card-skeleton.tsx
  </files>
  <action>
    1. Install missing shadcn components:
       ```bash
       npx shadcn@latest add badge select slider skeleton --yes
       ```
       This creates the 4 component files in `src/components/ui/`.

    2. Create `src/app/(dashboard)/dashboard/lead-card.tsx` as a server component (no "use client"):
       - Props: `{ lead: EnrichedLead }`
       - Render a Card (shadcn) containing:
         - **Header row**: Project type badge (left), freshness badge with color coding (right) -- "New" = green/default, "This Week" = blue/secondary, "Older" = gray/outline
         - **Title**: `lead.address` (or `lead.formattedAddress` if available, fallback to `lead.address`)
         - **Score indicator**: Circular or bar indicator showing score 0-100 with color gradient (green 70+, yellow 40-69, red 0-39). Keep simple -- a colored badge with the number is fine.
         - **Description**: Truncated to 2 lines with `line-clamp-2`
         - **Equipment tags**: Map `lead.inferredEquipment` to small Badge components (outline variant). Show max 4, then "+N more" if overflow.
         - **Footer row**: Distance (e.g., "12 mi away"), permit date if available, applicant/contractor name if available
         - **Link**: Wrap entire card in `<Link href={/dashboard/leads/${lead.id}}>` for click-through to detail
       - Use lucide-react icons: MapPin for distance, Calendar for date, User for applicant

    3. Create `src/app/(dashboard)/dashboard/lead-card-skeleton.tsx`:
       - Matches the visual structure of lead-card using Skeleton components
       - Show 3 skeleton cards stacked vertically (the loading state for the feed)
       - Export as `LeadCardSkeleton` for use in Suspense fallback
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - shadcn badge, select, slider, skeleton components installed
    - LeadCard renders all lead information (address, score, freshness, equipment, distance)
    - LeadCardSkeleton provides loading state matching card structure
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Lead filter controls, dashboard feed page, and navigation update</name>
  <files>
    src/app/(dashboard)/dashboard/lead-filters.tsx,
    src/app/(dashboard)/dashboard/page.tsx,
    src/app/(dashboard)/layout.tsx
  </files>
  <action>
    1. Create `src/app/(dashboard)/dashboard/lead-filters.tsx` as a CLIENT component ("use client"):
       - Props: `{ defaultRadius: number, dealerEquipment: string[] }`
       - `defaultRadius` is the company's serviceRadiusMiles (used as slider default when no URL param)
       - `dealerEquipment` is the company's configured equipment types (used to highlight matching types)
       - **Equipment filter**: Render EQUIPMENT_TYPES as a grid of checkboxes (or a multi-select dropdown using shadcn Select). Unchecked = show all (default). Checked items narrow the feed.
         - Use checkboxes in a collapsible section for clarity. Label: "Filter by Equipment Need"
         - Read initial state from `useSearchParams()` -- parse `equipment` param as comma-separated string
         - On change, update URL: `router.replace(pathname + "?" + params.toString())`
         - When all unchecked, remove `equipment` param entirely (show-all)
       - **Radius slider**: Render shadcn Slider with range 10-500, step 10
         - Label: "Search Radius: {value} miles"
         - Read initial value from `useSearchParams()` -- parse `radius` param, default to `defaultRadius`
         - Use local state for drag display, only update URL on `onValueCommit` (pointer release) to avoid excessive re-renders (Pitfall 4 from research)
         - Update URL param `radius` on commit
       - Wrap in a sticky sidebar or top filter bar depending on layout. Use a `<div>` with appropriate Tailwind classes. On mobile, show as a collapsible filter panel.

    2. Replace `src/app/(dashboard)/dashboard/page.tsx` entirely:
       - This is a SERVER component (async function, no "use client")
       - `searchParams` prop: `Promise<{ [key: string]: string | string[] | undefined }>` -- MUST await (Next.js 16)
       - Parse filter params:
         - `equipment`: split comma-separated string into string[] (empty = show all)
         - `radius`: parse as number (undefined = use company default)
       - Get session via `auth.api.getSession({ headers: await headers() })`
       - Query company profile for active org to get hqLat, hqLng, serviceRadiusMiles, equipmentTypes
       - Guard: if company profile missing hqLat/hqLng, show a message asking to update company settings with HQ location
       - Call `getFilteredLeads({ hqLat, hqLng, serviceRadiusMiles, dealerEquipment: profile.equipmentTypes, radiusMiles: parsedRadius, equipmentFilter: parsedEquipment })`
       - Render layout:
         - Page title: "Lead Feed" with subtitle showing count ("23 leads within 50 miles")
         - Two-column layout on desktop (lg:): filters sidebar (left, ~280px), lead cards (right, flex-1)
         - Single column on mobile: filters collapsible at top, cards below
         - `<LeadFilters>` component with defaultRadius and dealerEquipment props
         - Map `leads` to `<LeadCard>` components
         - **Empty state**: When leads array is empty, show a Card with friendly message: "No leads found matching your filters. Try expanding your search radius or clearing equipment filters." Include a link to settings if hqLat/hqLng is missing.
       - Wrap the lead card list in `<Suspense fallback={<LeadCardSkeleton />}>` if using streaming. Alternatively, since data is fetched in the page server component directly, the skeleton may not be needed for Suspense but is useful for loading.tsx.
       - Set metadata: `title: "Lead Feed | HeavyLeads"`

    3. Update `src/app/(dashboard)/layout.tsx` navigation:
       - Change the "Dashboard" nav link text to "Leads" (it already points to `/dashboard`)
       - Keep the link href as `/dashboard` (same route, better label for the primary feature)
       - This is a minimal change -- just rename the label in the sidebar nav
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - Dashboard page replaced with lead feed showing scored, filtered lead cards
    - Equipment multi-select filter updates URL params and narrows results
    - Radius slider updates URL on commit (not during drag) and filters by distance
    - Filter state persists in URL (bookmarkable, server-rendered)
    - Empty state displayed when no leads match filters
    - Sidebar nav shows "Leads" as primary navigation item
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit`
- Dashboard page imports and uses getFilteredLeads from Plan 01's queries module
- Filter controls update URL search params correctly
- Lead cards display all required information (address, score, freshness, equipment, distance)
- Empty state handles zero-result gracefully
</verification>

<success_criteria>
- Dashboard at `/dashboard` shows a card-based lead feed sorted by score
- Each card shows: address, project type, freshness badge, score, inferred equipment, distance
- Equipment filter narrows leads by inferred equipment type (show-all default)
- Radius slider controls geographic scope (10-500 miles, defaults to company setting)
- URL search params reflect current filter state
- Empty state provides helpful guidance when no leads match
- Sidebar navigation updated to show "Leads"
</success_criteria>

<output>
After completion, create `.planning/phases/03-lead-intelligence-and-dashboard/03-02-SUMMARY.md`
</output>
