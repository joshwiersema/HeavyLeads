---
phase: 24-groundpulse-rebrand-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # Layouts and nav
  - src/app/layout.tsx
  - src/app/(auth)/layout.tsx
  - src/app/(dashboard)/layout.tsx
  - src/app/(billing)/layout.tsx
  - src/app/(onboarding)/onboarding/page.tsx
  - src/components/dashboard/mobile-nav.tsx
  # Auth forms
  - src/components/auth/sign-in-form.tsx
  - src/components/auth/sign-up-form.tsx
  # Page metadata
  - src/app/(auth)/sign-in/page.tsx
  - src/app/(auth)/sign-up/page.tsx
  - src/app/(auth)/forgot-password/page.tsx
  - src/app/(auth)/reset-password/page.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/(dashboard)/dashboard/leads/[id]/page.tsx
  - src/app/(dashboard)/dashboard/bookmarks/page.tsx
  - src/app/(dashboard)/dashboard/saved-searches/page.tsx
  - src/app/(dashboard)/settings/layout.tsx
  - src/app/(billing)/billing/page.tsx
  - src/app/contact/page.tsx
  - src/app/terms/page.tsx
  - src/app/privacy/page.tsx
  - src/app/unsubscribe/page.tsx
  # Email templates
  - src/components/emails/email-layout.tsx
  - src/components/emails/daily-digest.tsx
  - src/components/emails/weekly-summary.tsx
  - src/components/emails/welcome.tsx
  - src/components/emails/verify-email.tsx
  - src/components/emails/storm-alert.tsx
  - src/components/emails/password-reset.tsx
  # Email senders
  - src/lib/email/send-digest.ts
  - src/lib/email/send-weekly-summary.ts
  - src/lib/email/send-storm-alert.ts
  # Auth and billing
  - src/lib/auth.ts
  - src/lib/stripe.ts
  - src/actions/onboarding.ts
  - src/components/onboarding/wizard-shell.tsx
  - src/components/billing/billing-status.tsx
  - src/components/billing/plan-selector.tsx
  - src/components/dashboard/empty-state.tsx
  # Scraper user-agents
  - src/lib/scraper/adapters/ferc-energy.ts
  - src/lib/scraper/adapters/nws-storm-adapter.ts
  - src/app/api/cron/storm-alerts/route.ts
  # Tests
  - tests/regressions/landing-page.test.tsx
  - tests/regressions/mobile-nav.test.tsx
  - tests/regressions/sign-in-redirect.test.tsx
  - tests/auth/send-reset-password.test.ts
  - tests/billing/billing-page.test.tsx
  - tests/email/digest-templates.test.ts
  - tests/email/unsubscribe.test.ts
  - tests/storm-alerts/storm-email.test.ts
  - tests/scraper/nws-storm-adapter.test.ts
  # Config and scripts
  - package.json
  - CLAUDE.md
  - scripts/stripe-seed.ts
  - scripts/db-reset.ts
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03
  - BRAND-04

must_haves:
  truths:
    - "Zero occurrences of 'HeavyLeads' in any src/ file"
    - "Zero occurrences of 'LeadForge' or 'leadforge' in any src/ file"
    - "Zero occurrences of 'HeavyLeads' in any test file"
    - "Logo mark displays 'GP' instead of 'H' in header, sidebar, auth, mobile nav, and onboarding"
    - "All email templates say 'GroundPulse' in branding"
    - "OG metadata, page titles, and Twitter cards say 'GroundPulse'"
    - "Scraper User-Agent headers say 'GroundPulse'"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root metadata with GroundPulse branding"
      contains: "GroundPulse"
    - path: "src/components/emails/email-layout.tsx"
      provides: "Shared email layout with GroundPulse branding"
      contains: "GroundPulse"
    - path: "CLAUDE.md"
      provides: "Updated project guide"
      contains: "GroundPulse"
  key_links:
    - from: "all src/**/*.tsx"
      to: "GroundPulse brand"
      via: "text replacement"
      pattern: "GroundPulse"
---

<objective>
Rebrand the entire codebase from HeavyLeads/LeadForge to GroundPulse.

Purpose: The product name is changing to GroundPulse. Every user-facing surface, email template, metadata tag, User-Agent string, test assertion, and config file must reflect the new name. The logo monogram changes from "H" to "GP".

Output: Zero references to HeavyLeads or LeadForge remain in src/, tests/, scripts/, or root config files.
</objective>

<execution_context>
@C:/Users/joshu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joshu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-groundpulse-rebrand-landing-page/24-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all HeavyLeads/LeadForge references with GroundPulse across src/, tests/, scripts/</name>
  <files>
    src/app/layout.tsx,
    src/app/(auth)/layout.tsx,
    src/app/(dashboard)/layout.tsx,
    src/app/(billing)/layout.tsx,
    src/app/(onboarding)/onboarding/page.tsx,
    src/components/dashboard/mobile-nav.tsx,
    src/components/auth/sign-in-form.tsx,
    src/components/auth/sign-up-form.tsx,
    src/app/(auth)/sign-in/page.tsx,
    src/app/(auth)/sign-up/page.tsx,
    src/app/(auth)/forgot-password/page.tsx,
    src/app/(auth)/reset-password/page.tsx,
    src/app/(dashboard)/dashboard/page.tsx,
    src/app/(dashboard)/dashboard/leads/[id]/page.tsx,
    src/app/(dashboard)/dashboard/bookmarks/page.tsx,
    src/app/(dashboard)/dashboard/saved-searches/page.tsx,
    src/app/(dashboard)/settings/layout.tsx,
    src/app/(billing)/billing/page.tsx,
    src/app/contact/page.tsx,
    src/app/terms/page.tsx,
    src/app/privacy/page.tsx,
    src/app/unsubscribe/page.tsx,
    src/components/emails/email-layout.tsx,
    src/components/emails/daily-digest.tsx,
    src/components/emails/weekly-summary.tsx,
    src/components/emails/welcome.tsx,
    src/components/emails/verify-email.tsx,
    src/components/emails/storm-alert.tsx,
    src/components/emails/password-reset.tsx,
    src/lib/email/send-digest.ts,
    src/lib/email/send-weekly-summary.ts,
    src/lib/email/send-storm-alert.ts,
    src/lib/auth.ts,
    src/lib/stripe.ts,
    src/actions/onboarding.ts,
    src/components/onboarding/wizard-shell.tsx,
    src/components/billing/billing-status.tsx,
    src/components/billing/plan-selector.tsx,
    src/components/dashboard/empty-state.tsx,
    src/lib/scraper/adapters/ferc-energy.ts,
    src/lib/scraper/adapters/nws-storm-adapter.ts,
    src/app/api/cron/storm-alerts/route.ts,
    tests/regressions/landing-page.test.tsx,
    tests/regressions/mobile-nav.test.tsx,
    tests/regressions/sign-in-redirect.test.tsx,
    tests/auth/send-reset-password.test.ts,
    tests/billing/billing-page.test.tsx,
    tests/email/digest-templates.test.ts,
    tests/email/unsubscribe.test.ts,
    tests/storm-alerts/storm-email.test.ts,
    tests/scraper/nws-storm-adapter.test.ts,
    package.json,
    CLAUDE.md,
    scripts/stripe-seed.ts,
    scripts/db-reset.ts
  </files>
  <action>
    Perform a systematic find-and-replace across every file listed above. Read each file, apply the replacements, write it back.

    **Text replacements (in this priority order):**

    1. `"HeavyLeads Standard"` -> `"GroundPulse Standard"` (Stripe product name)
    2. `heavyleads_standard_monthly` -> `groundpulse_standard_monthly` (Stripe lookup key)
    3. `heavyleads_setup_fee` -> `groundpulse_setup_fee` (Stripe lookup key)
    4. `"name": "heavyleads"` -> `"name": "groundpulse"` (package.json)
    5. `HeavyLeads/1.0` -> `GroundPulse/1.0` (User-Agent strings)
    6. `heavyleads.com` -> `groundpulse.com` (User-Agent, domain references)
    7. `HeavyLeads <` -> `GroundPulse <` (email from addresses)
    8. `HeavyLeads |` -> `GroundPulse |` (email footer text)
    9. `HeavyLeads.` -> `GroundPulse.` (email footer text with period)
    10. `to HeavyLeads` -> `to GroundPulse` (welcome/auth copy)
    11. `with HeavyLeads` -> `with GroundPulse` (billing/plan copy)
    12. `HeavyLeads` -> `GroundPulse` (all remaining text occurrences)
    13. `LeadForge` -> `GroundPulse` (one occurrence in storm-alerts/route.ts: `app.leadforge.com`)
    14. `leadforge` -> `groundpulse` (any lowercase references)
    15. `heavyleads` -> `groundpulse` (any remaining lowercase like package name)

    **For CLAUDE.md specifically:**
    - Change the title from `# HeavyLeads - Claude Code Guide` to `# GroundPulse - Claude Code Guide`
    - Change the project description from "B2B SaaS platform for construction lead intelligence..." — keep the same text but ensure no HeavyLeads references remain

    **For src/app/layout.tsx metadata:**
    - title: `"GroundPulse"`
    - description: `"GroundPulse aggregates construction permits, government bids, and project news into a single scored feed — so your sales team reaches the right jobsite first."`
    - openGraph title: `"GroundPulse - Construction Lead Intelligence for Every Trade"`
    - openGraph description: same as above
    - openGraph siteName: `"GroundPulse"`
    - openGraph image alt: `"GroundPulse - Construction Lead Intelligence"`
    - twitter title: `"GroundPulse - Construction Lead Intelligence for Every Trade"`
    - twitter description: same as above

    **For email templates:**
    - All brand text: `HeavyLeads` -> `GroundPulse`
    - All JSDoc comments: `HeavyLeads` -> `GroundPulse`
    - Footer text: `HeavyLeads | Austin, TX` -> `GroundPulse | United States` (or keep existing if already updated)

    **For tests:**
    - Update all string assertions to match new brand name (e.g., `"Reset your GroundPulse password"`, `"GroundPulse Standard"`, etc.)
    - Update test descriptions if they reference "HeavyLeads" (e.g., `"renders the HeavyLeads brand link"` -> `"renders the GroundPulse brand link"`)

    **Do NOT touch:** `src/app/page.tsx` (the landing page — handled by Plan 02).
  </action>
  <verify>
    <automated>cd C:/Users/joshu/Documents/HeavyLeadsApp && export PATH="/c/Program Files/nodejs:$PATH" && grep -r "HeavyLeads\|heavyleads\|LeadForge\|leadforge" src/ tests/ scripts/ CLAUDE.md package.json --include="*.ts" --include="*.tsx" --include="*.json" --include="*.md" | grep -v "node_modules" | grep -v ".planning" | grep -v "page.tsx:.*GroundPulse"</automated>
  </verify>
  <done>
    Zero occurrences of HeavyLeads, heavyleads, LeadForge, or leadforge in src/, tests/, scripts/, CLAUDE.md, or package.json (excluding the landing page src/app/page.tsx which Plan 02 handles).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update logo monogram from "H" to "GP" across all layouts</name>
  <files>
    src/app/(auth)/layout.tsx,
    src/app/(dashboard)/layout.tsx,
    src/app/(billing)/layout.tsx,
    src/app/(onboarding)/onboarding/page.tsx,
    src/components/dashboard/mobile-nav.tsx
  </files>
  <action>
    Update every instance of the logo monogram mark. The current pattern is a small div with "H" inside a gold gradient background. Change the letter to "GP" and adjust sizing slightly to accommodate 2 characters.

    **Current pattern (varies by context):**
    ```tsx
    <div className="flex size-7 items-center justify-center rounded-lg bg-gradient-to-br from-amber-400 to-amber-600 text-xs font-black text-[#1a1a1e]">
      H
    </div>
    ```

    **New pattern:**
    ```tsx
    <div className="flex size-7 items-center justify-center rounded-lg bg-gradient-to-br from-amber-400 to-amber-600 text-[10px] font-black tracking-tight text-[#1a1a1e]">
      GP
    </div>
    ```

    Apply this to every logo mark instance. There are multiple size variants across files:

    - **Dashboard sidebar** (`src/app/(dashboard)/layout.tsx`): `size-7`, appears twice (desktop sidebar line ~89 and mobile topbar line ~108). Desktop uses `text-xs`, mobile uses `text-[10px]`. Change both inner text from `H` to `GP`. For the `size-7` variant use `text-[10px]`, for `size-6` variant use `text-[9px]`.
    - **Auth layout** (`src/app/(auth)/layout.tsx`): `size-7` desktop (line ~17) and `size-6` mobile (line ~77). Change both to `GP`.
    - **Billing layout** (`src/app/(billing)/layout.tsx`): Find the logo mark and change `H` to `GP`.
    - **Onboarding page** (`src/app/(onboarding)/onboarding/page.tsx`): Find the logo mark and change `H` to `GP`.
    - **Mobile nav** (`src/components/dashboard/mobile-nav.tsx`): Find the logo mark and change `H` to `GP`.

    For all monograms: add `tracking-tight` to the className if not already present (tightens letter spacing for the 2-char monogram).
  </action>
  <verify>
    <automated>cd C:/Users/joshu/Documents/HeavyLeadsApp && export PATH="/c/Program Files/nodejs:$PATH" && grep -rn "font-black" src/app/\(auth\)/layout.tsx src/app/\(dashboard\)/layout.tsx src/app/\(billing\)/layout.tsx src/app/\(onboarding\)/onboarding/page.tsx src/components/dashboard/mobile-nav.tsx | grep -c "GP"</automated>
  </verify>
  <done>
    Every logo monogram div that previously showed "H" now shows "GP" with appropriate font sizing (text-[10px] or text-[9px] depending on container size) and tracking-tight for letter spacing.
  </done>
</task>

</tasks>

<verification>
Run these checks after both tasks complete:

1. `grep -r "HeavyLeads\|heavyleads\|LeadForge\|leadforge" src/ tests/ scripts/ CLAUDE.md package.json --include="*.ts" --include="*.tsx" --include="*.json" --include="*.md" | grep -v node_modules | grep -v .planning` — must return zero results (or only src/app/page.tsx which Plan 02 replaces)
2. `grep -rn "font-black" src/ --include="*.tsx" | grep -v page.tsx` — every logo mark instance should contain "GP"
3. TypeScript compilation: `npx tsc --noEmit` passes
</verification>

<success_criteria>
- Zero references to HeavyLeads, heavyleads, LeadForge, or leadforge remain in production code, test files, or config files
- All logo monograms display "GP" instead of "H"
- All email templates brand as GroundPulse
- All page metadata titles use "GroundPulse"
- All User-Agent strings use "GroundPulse/1.0"
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/24-groundpulse-rebrand-landing-page/24-01-SUMMARY.md`
</output>
