---
phase: 24-groundpulse-rebrand-landing-page
plan: 03
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
files_modified:
  - tests/regressions/landing-page.test.tsx
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03
  - BRAND-04
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04
  - LAND-05

must_haves:
  truths:
    - "Zero references to HeavyLeads or LeadForge remain in entire src/, tests/, scripts/ tree"
    - "TypeScript compilation succeeds with no errors"
    - "All existing tests pass with updated brand assertions"
    - "Landing page test validates GroundPulse brand name renders"
  artifacts:
    - path: "tests/regressions/landing-page.test.tsx"
      provides: "Updated landing page regression test"
      contains: "GroundPulse"
  key_links:
    - from: "tests/"
      to: "src/"
      via: "test assertions match production brand"
      pattern: "GroundPulse"
---

<objective>
Run full verification sweep to confirm zero old brand references remain, TypeScript compiles, and tests pass with updated assertions.

Purpose: Plans 01 and 02 touched 55+ files. This plan ensures nothing was missed and the codebase is coherent.

Output: Verified clean build with all tests passing under GroundPulse branding.
</objective>

<execution_context>
@C:/Users/joshu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joshu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-groundpulse-rebrand-landing-page/24-01-SUMMARY.md
@.planning/phases/24-groundpulse-rebrand-landing-page/24-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-codebase brand sweep and build verification</name>
  <files>tests/regressions/landing-page.test.tsx</files>
  <action>
    **Step 1: Full grep sweep for any remaining old brand references.**

    Run these commands and fix ANY remaining occurrences:
    ```bash
    grep -rn "HeavyLeads\|heavyleads\|LeadForge\|leadforge" src/ tests/ scripts/ CLAUDE.md package.json --include="*.ts" --include="*.tsx" --include="*.json" --include="*.md"
    ```

    If any results are found, fix them by replacing with GroundPulse/groundpulse as appropriate.

    Also check for the old "H" monogram letter in logo divs (should all be "GP" now):
    ```bash
    grep -n "font-black" src/ -r --include="*.tsx" | grep -v "GP"
    ```

    **Step 2: Update landing page test if needed.**

    The landing page test (`tests/regressions/landing-page.test.tsx`) must validate the new page structure. The test should assert:
    - "GroundPulse" brand name renders (not "HeavyLeads")
    - Key sections exist (industries, stats, etc.)

    If Plan 01 already updated the test assertions from "HeavyLeads" to "GroundPulse", verify they are correct. If the landing page test still references old content structure (like "Everything You Need to Win More Deals"), update to match the new page content.

    **Step 3: TypeScript compilation check.**
    ```bash
    npx tsc --noEmit
    ```
    Fix any type errors.

    **Step 4: Run test suite.**
    ```bash
    npm test
    ```
    Fix any failing tests. Common failures will be assertion mismatches where test strings still reference old brand or old landing page copy.
  </action>
  <verify>
    <automated>cd C:/Users/joshu/Documents/HeavyLeadsApp && export PATH="/c/Program Files/nodejs:$PATH" && grep -rnc "HeavyLeads\|heavyleads\|LeadForge\|leadforge" src/ tests/ scripts/ CLAUDE.md package.json --include="*.ts" --include="*.tsx" --include="*.json" --include="*.md" | grep -v ":0$" | wc -l</automated>
  </verify>
  <done>
    - grep returns 0 files with old brand references across src/, tests/, scripts/, CLAUDE.md, package.json
    - TypeScript compiles with zero errors (`npx tsc --noEmit` exits 0)
    - All tests pass (`npm test` exits 0)
    - Landing page test asserts "GroundPulse" renders
  </done>
</task>

</tasks>

<verification>
Final verification checklist:

1. `grep -r "HeavyLeads\|heavyleads\|LeadForge\|leadforge" src/ tests/ scripts/ CLAUDE.md package.json --include="*.ts" --include="*.tsx" --include="*.json" --include="*.md"` returns NO results
2. `npx tsc --noEmit` exits with code 0
3. `npm test` all tests pass
4. `grep "GroundPulse" src/app/page.tsx` shows brand in landing page
5. `grep "GroundPulse" src/app/layout.tsx` shows brand in metadata
6. `grep "GroundPulse" src/components/emails/email-layout.tsx` shows brand in emails
</verification>

<success_criteria>
- Zero old brand references in any source, test, or config file
- Clean TypeScript compilation
- All tests pass with updated assertions
- The GroundPulse rebrand is complete and the landing page is live
</success_criteria>

<output>
After completion, create `.planning/phases/24-groundpulse-rebrand-landing-page/24-03-SUMMARY.md`
</output>
