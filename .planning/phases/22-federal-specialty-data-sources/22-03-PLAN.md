---
phase: 22-federal-specialty-data-sources
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/lib/scraper/adapters/ferc-energy.ts
  - src/lib/scraper/adapters/fcc-antenna.ts
  - src/lib/scraper/adapters/index.ts
autonomous: true
requirements: [FED-05, FED-06]

must_haves:
  truths:
    - "FERC adapter fetches energy infrastructure filings (pipeline, power plant construction)"
    - "FCC adapter fetches antenna structure registrations with tower coordinates"
    - "All 6 new adapters are registered in index.ts for the appropriate industries"
    - "Both adapters gracefully return [] on API error"
  artifacts:
    - path: "src/lib/scraper/adapters/ferc-energy.ts"
      provides: "FERC energy infrastructure filings adapter"
      exports: ["FercEnergyAdapter"]
    - path: "src/lib/scraper/adapters/fcc-antenna.ts"
      provides: "FCC antenna structure registration adapter"
      exports: ["FccAntennaAdapter"]
    - path: "src/lib/scraper/adapters/index.ts"
      provides: "Updated adapter registry with all 6 new federal adapters"
      exports: ["getAdaptersForIndustry", "getAllAdapters"]
  key_links:
    - from: "src/lib/scraper/adapters/ferc-energy.ts"
      to: "elibrary.ferc.gov"
      via: "fetch FERC eLibrary RSS or search"
      pattern: "ferc\\.gov"
    - from: "src/lib/scraper/adapters/fcc-antenna.ts"
      to: "opendata.fcc.gov"
      via: "Socrata API for antenna registrations"
      pattern: "fcc\\.gov"
    - from: "src/lib/scraper/adapters/index.ts"
      to: "all 6 new adapter files"
      via: "import and register in getHardcodedAdapters"
      pattern: "UsaSpendingContractsAdapter|OshaInspectionsAdapter|EpaBrownfieldsAdapter|GrantsGovAdapter|FercEnergyAdapter|FccAntennaAdapter"
---

<objective>
Implement the FERC energy infrastructure filings adapter and FCC antenna structure registration adapter, then register all 6 new federal adapters in the adapter index for appropriate industries.

Purpose: FERC and FCC data complete the 6-source federal adapter set. Registration in index.ts makes all adapters available to the scraping pipeline.

Output: 2 working adapter files and updated index.ts with all 6 new adapters registered per industry.
</objective>

<execution_context>
@C:/Users/joshu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joshu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/22-federal-specialty-data-sources/22-CONTEXT.md

<interfaces>
<!-- Source types and rate limiters from Plan 22-01 -->

From src/lib/scraper/adapters/base-adapter.ts (after 22-01):
```typescript
export const sourceTypes = [..., "contract-award", "inspection", "brownfield", "grant", "energy", "telecom"] as const;
export type SourceType = (typeof sourceTypes)[number];
export interface ScraperAdapter { sourceId, sourceName, sourceType, jurisdiction?, scrape(): Promise<RawLeadData[]> }
```

From src/lib/scraper/api-rate-limiter.ts (after 22-01):
```typescript
export async function getFercQueue(): Promise<PQueue>;  // concurrency=1, 5/min
export async function getFccQueue(): Promise<PQueue>;   // concurrency=1, 10/min
```

From src/lib/scraper/adapters/index.ts (current structure):
```typescript
import { SamGovBidsAdapter } from "./sam-gov-bids";
// ... other imports
export async function getAdaptersForIndustry(industry: Industry): Promise<ScraperAdapter[]> { ... }
function getHardcodedAdapters(industry: Industry): ScraperAdapter[] {
  switch (industry) {
    case "heavy_equipment": return [ ... ];
    case "hvac": return [ ... ];
    case "roofing": return [ ... ];
    case "solar": return [ ... ];
    case "electrical": return [ ... ];
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FERC energy filings adapter and FCC antenna structure adapter</name>
  <files>src/lib/scraper/adapters/ferc-energy.ts, src/lib/scraper/adapters/fcc-antenna.ts</files>
  <read_first>
    - src/lib/scraper/adapters/fema-disaster-adapter.ts (no-auth federal API pattern)
    - src/lib/scraper/adapters/sam-gov-bids.ts (rate-limited API pattern)
    - src/lib/scraper/adapters/base-adapter.ts (RawLeadData schema)
    - src/lib/scraper/api-rate-limiter.ts (getFercQueue, getFccQueue)
    - src/lib/scraper/adapters/utils.ts (extractLocation, toTitleCase)
  </read_first>
  <action>
1. Create `ferc-energy.ts`:
   - Class: `FercEnergyAdapter implements ScraperAdapter`
   - sourceId: `"ferc-energy"`
   - sourceName: `"FERC Energy Infrastructure Filings"`
   - sourceType: `"energy" as const`
   - No auth required

   API approach: FERC eLibrary RSS feed
   - Primary endpoint: `https://elibrary.ferc.gov/eLibrary/filelist?docket_number=&accession_number=&category=issuance&resultFormat=rss`
   - If RSS fails, try the FERC Documents REST-style search:
     `https://elibrary.ferc.gov/eLibrary/search?docket_number=&category=issuance&resultFormat=xml`
   - Filter for construction-related terms in title/description: "construction", "pipeline", "power plant", "transmission", "substation", "facility", "infrastructure", "LNG", "solar", "wind"
   - Parse RSS XML response manually (no new dependencies -- use regex or DOMParser-like approach):
     - Extract <item> elements with <title>, <link>, <description>, <pubDate>
     - Simple regex approach: match `<item>.*?<title>(.*?)</title>.*?<link>(.*?)</link>.*?<description>(.*?)</description>.*?<pubDate>(.*?)</pubDate>.*?</item>` with `s` flag

   Map to RawLeadData:
   - externalId: Extract docket number from link URL or use `ferc-${hash_of_link}`
   - title: item title (truncate to 200 chars)
   - description: item description (strip HTML tags, truncate to 500 chars)
   - postedDate: new Date(item pubDate)
   - sourceUrl: item link
   - sourceType: "energy"
   - Use extractLocation from utils.ts on description for city/state

   Use `getFercQueue()` rate limiter. Wrap in try/catch, return [] on failure.

2. Create `fcc-antenna.ts`:
   - Class: `FccAntennaAdapter implements ScraperAdapter`
   - sourceId: `"fcc-antenna"`
   - sourceName: `"FCC Antenna Structure Registrations"`
   - sourceType: `"telecom" as const`
   - No auth required

   API approach: FCC Open Data portal uses Socrata
   - Endpoint: `https://opendata.fcc.gov/resource/2fwp-vbpn.json` (ASR registrations dataset)
   - If that dataset ID fails (404), try alternate approach:
     - `https://opendata.fcc.gov/resource/i3wh-hgsf.json` (alternate ASR dataset ID)
   - Query params for Socrata SODA API:
     - `$where=reg_dat > '${90_days_ago_ISO}'` (recently registered)
     - `$limit=200`
     - `$order=reg_dat DESC`
   - App token optional (Socrata throttles anonymous to 1000/hr which is fine)

   Map response array to RawLeadData:
   - externalId: `fcc-asr-${item.reg_num || item.asr_num || item.unique_si}`
   - title: `Antenna Structure Registration: ${item.entity_name || item.owner_name || "Unknown"} - ${toTitleCase(item.city_name || item.city || "")}, ${item.state_code || item.state || ""}`
   - description: `FCC antenna structure registration. Height: ${item.struc_hgt || item.overall_hg || "N/A"} ft. Type: ${item.struc_typ || "N/A"}.`
   - lat: parseFloat(item.lat_deg + item.lat_min/60 + item.lat_sec/3600) OR parseFloat(item.latitude) if decimal format
   - lng: negative of parseFloat(item.long_deg + item.long_min/60 + item.long_sec/3600) OR parseFloat(item.longitude) if decimal
   - city: item.city_name || item.city
   - state: item.state_code || item.state
   - postedDate: new Date(item.reg_dat || item.grant_date)
   - sourceUrl: `https://wireless2.fcc.gov/UlsApp/AsrSearch/asrRegistration.jsp?regKey=${item.reg_num}`
   - sourceType: "telecom"

   Use `getFccQueue()` rate limiter. Wrap in try/catch, return [] on failure with console.warn.
  </action>
  <verify>
    <automated>cd "C:/Users/joshu/Documents/HeavyLeadsApp" && npx tsc --noEmit src/lib/scraper/adapters/ferc-energy.ts src/lib/scraper/adapters/fcc-antenna.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ferc-energy.ts exports FercEnergyAdapter implementing ScraperAdapter
    - FercEnergyAdapter.sourceType === "energy"
    - FercEnergyAdapter.scrape() fetches from FERC eLibrary RSS, filters for construction terms
    - fcc-antenna.ts exports FccAntennaAdapter implementing ScraperAdapter
    - FccAntennaAdapter.sourceType === "telecom"
    - FccAntennaAdapter.scrape() fetches from FCC Socrata open data with SODA query params
    - FCC adapter extracts lat/lng coordinates from registration data
    - Both adapters return RawLeadData[] and handle errors gracefully
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>FERC energy adapter and FCC antenna adapter exist, implement ScraperAdapter, target correct APIs, extract coordinates where available, handle errors gracefully</done>
</task>

<task type="auto">
  <name>Task 2: Register all 6 new adapters in adapter index</name>
  <files>src/lib/scraper/adapters/index.ts</files>
  <read_first>
    - src/lib/scraper/adapters/index.ts (current adapter registry and industry mapping)
    - src/lib/scraper/adapters/usaspending-contracts.ts (from 22-01)
    - src/lib/scraper/adapters/osha-inspections.ts (from 22-01)
    - src/lib/scraper/adapters/epa-brownfields.ts (from 22-02)
    - src/lib/scraper/adapters/grants-gov.ts (from 22-02)
    - src/lib/scraper/adapters/ferc-energy.ts (from this plan Task 1)
    - src/lib/scraper/adapters/fcc-antenna.ts (from this plan Task 1)
  </read_first>
  <action>
Update `index.ts` to import and register all 6 new federal adapters:

1. Add 6 new imports at the top:
   ```typescript
   import { UsaSpendingContractsAdapter } from "./usaspending-contracts";
   import { OshaInspectionsAdapter } from "./osha-inspections";
   import { EpaBrownfieldsAdapter } from "./epa-brownfields";
   import { GrantsGovAdapter } from "./grants-gov";
   import { FercEnergyAdapter } from "./ferc-energy";
   import { FccAntennaAdapter } from "./fcc-antenna";
   ```

2. Update `getHardcodedAdapters()` switch cases to include new adapters per industry:

   **heavy_equipment** -- add all 6 (construction contracts, inspections, brownfields, grants, energy infra, telecom towers are all relevant):
   - `new UsaSpendingContractsAdapter()` (NAICS 236/237/238 already built-in)
   - `new OshaInspectionsAdapter()`
   - `new EpaBrownfieldsAdapter()`
   - `new GrantsGovAdapter()`
   - `new FercEnergyAdapter()`
   - `new FccAntennaAdapter()`

   **hvac** -- add contracts, inspections, grants (relevant for mechanical trade):
   - `new UsaSpendingContractsAdapter()` (uses default NAICS which includes 238)
   - `new OshaInspectionsAdapter()`
   - `new GrantsGovAdapter()`

   **roofing** -- add contracts, inspections, brownfields, grants:
   - `new UsaSpendingContractsAdapter()`
   - `new OshaInspectionsAdapter()`
   - `new EpaBrownfieldsAdapter()`
   - `new GrantsGovAdapter()`

   **solar** -- add contracts, grants, energy filings (FERC highly relevant):
   - `new UsaSpendingContractsAdapter()`
   - `new GrantsGovAdapter()`
   - `new FercEnergyAdapter()`

   **electrical** -- add contracts, inspections, grants, energy, telecom (FCC highly relevant):
   - `new UsaSpendingContractsAdapter()`
   - `new OshaInspectionsAdapter()`
   - `new GrantsGovAdapter()`
   - `new FercEnergyAdapter()`
   - `new FccAntennaAdapter()`

   Rationale for industry mapping:
   - USAspending: Relevant to ALL industries (federal construction contracts)
   - OSHA: Relevant to all trades with physical jobsites (not solar since primarily residential)
   - EPA Brownfields: Heavy equipment (site clearing), roofing (remediation buildings)
   - Grants.gov: ALL industries (federal funding for construction projects)
   - FERC: Solar (solar farms), electrical (substations, transmission), heavy equipment (pipeline/power plant construction)
   - FCC: Heavy equipment (tower construction), electrical (antenna/tower electrical work)
  </action>
  <verify>
    <automated>cd "C:/Users/joshu/Documents/HeavyLeadsApp" && npx tsc --noEmit src/lib/scraper/adapters/index.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - index.ts imports all 6 new adapter classes
    - heavy_equipment case includes all 6 new adapters
    - hvac case includes UsaSpendingContractsAdapter, OshaInspectionsAdapter, GrantsGovAdapter
    - roofing case includes UsaSpendingContractsAdapter, OshaInspectionsAdapter, EpaBrownfieldsAdapter, GrantsGovAdapter
    - solar case includes UsaSpendingContractsAdapter, GrantsGovAdapter, FercEnergyAdapter
    - electrical case includes UsaSpendingContractsAdapter, OshaInspectionsAdapter, GrantsGovAdapter, FercEnergyAdapter, FccAntennaAdapter
    - TypeScript compiles without errors
    - No existing adapters removed
  </acceptance_criteria>
  <done>All 6 new adapters are imported and registered in the appropriate industry cases in getHardcodedAdapters(); TypeScript compiles cleanly; existing adapters preserved</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all 3 modified/created files
- Both new adapters implement ScraperAdapter interface
- index.ts correctly maps adapters to industries based on relevance
- No existing adapter registrations removed or broken
</verification>

<success_criteria>
- FED-05: FercEnergyAdapter fetches from FERC eLibrary RSS, filters for construction-related energy filings
- FED-06: FccAntennaAdapter fetches from FCC Socrata open data, extracts tower coordinates
- All 6 new adapters registered in index.ts for appropriate industries
- Full TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/22-federal-specialty-data-sources/22-03-SUMMARY.md`
</output>
