---
phase: 02-scraping-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/lib/scraper/adapters/austin-permits.ts
  - src/lib/scraper/adapters/dallas-permits.ts
  - src/lib/scraper/adapters/atlanta-permits.ts
  - src/lib/scraper/adapters/index.ts
  - src/lib/scraper/scheduler.ts
  - src/app/api/scraper/run/route.ts
  - tests/scraper/adapters.test.ts
  - tests/scraper/scheduler.test.ts
autonomous: true
requirements: [DATA-01, DATA-05, DATA-07]

must_haves:
  truths:
    - "Austin adapter fetches permits from Socrata SODA API and maps to RawPermitData including lat/lng from source"
    - "Dallas adapter fetches permits from Socrata SODA API and maps to RawPermitData (requires geocoding since no coords in source)"
    - "Atlanta adapter fetches permits from ArcGIS REST API and maps to RawPermitData"
    - "All three adapters are registered and run when pipeline executes"
    - "Scheduler triggers pipeline daily at 06:00 UTC via node-cron"
    - "Manual pipeline run is triggerable via API route"
    - "New adapter can be added by creating a file and registering it — no changes to pipeline.ts needed"
  artifacts:
    - path: "src/lib/scraper/adapters/austin-permits.ts"
      provides: "Austin TX Socrata SODA adapter"
      exports: ["AustinPermitsAdapter"]
    - path: "src/lib/scraper/adapters/dallas-permits.ts"
      provides: "Dallas TX Socrata SODA adapter"
      exports: ["DallasPermitsAdapter"]
    - path: "src/lib/scraper/adapters/atlanta-permits.ts"
      provides: "Atlanta GA ArcGIS adapter"
      exports: ["AtlantaPermitsAdapter"]
    - path: "src/lib/scraper/adapters/index.ts"
      provides: "Adapter auto-registration"
      exports: ["initializeAdapters"]
    - path: "src/lib/scraper/scheduler.ts"
      provides: "Daily cron scheduler"
      exports: ["startScheduler"]
    - path: "src/app/api/scraper/run/route.ts"
      provides: "Manual trigger API endpoint"
      exports: ["POST"]
  key_links:
    - from: "src/lib/scraper/adapters/austin-permits.ts"
      to: "src/lib/scraper/adapters/base-adapter.ts"
      via: "implements ScraperAdapter"
      pattern: "implements ScraperAdapter"
    - from: "src/lib/scraper/adapters/dallas-permits.ts"
      to: "src/lib/scraper/adapters/base-adapter.ts"
      via: "implements ScraperAdapter"
      pattern: "implements ScraperAdapter"
    - from: "src/lib/scraper/adapters/atlanta-permits.ts"
      to: "src/lib/scraper/adapters/base-adapter.ts"
      via: "implements ScraperAdapter"
      pattern: "implements ScraperAdapter"
    - from: "src/lib/scraper/scheduler.ts"
      to: "src/lib/scraper/pipeline.ts"
      via: "node-cron triggers runPipeline"
      pattern: "runPipeline"
    - from: "src/app/api/scraper/run/route.ts"
      to: "src/lib/scraper/pipeline.ts"
      via: "POST handler calls runPipeline"
      pattern: "runPipeline"
---

<objective>
Implement three jurisdiction scraper adapters (Austin TX, Dallas TX, Atlanta GA), daily scheduler, and manual trigger API route. Wires adapters into the pipeline framework from Plan 01.

Purpose: Delivers the concrete data collection capability — the system actually scrapes permit data from 3 jurisdictions, geocodes locations, and stores lead records. This fulfills the core Phase 2 success criteria.

Output: Three working scraper adapters, auto-registration, daily scheduler, API trigger endpoint, and adapter unit tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scraping-pipeline/02-RESEARCH.md
@.planning/phases/02-scraping-pipeline/02-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 02-01 that this plan implements against -->

From src/lib/scraper/adapters/base-adapter.ts:
```typescript
import { z } from 'zod';

export const rawPermitSchema = z.object({
  permitNumber: z.string(),
  description: z.string().optional(),
  address: z.string(),
  projectType: z.string().optional(),
  estimatedValue: z.number().optional(),
  applicantName: z.string().optional(),
  permitDate: z.date().optional(),
  sourceUrl: z.string().optional(),
});

export type RawPermitData = z.infer<typeof rawPermitSchema>;

export interface ScraperAdapter {
  readonly sourceId: string;
  readonly sourceName: string;
  readonly jurisdiction: string;
  scrape(): Promise<RawPermitData[]>;
}
```

From src/lib/scraper/registry.ts:
```typescript
export function registerAdapter(adapter: ScraperAdapter): void;
export function getRegisteredAdapters(): ScraperAdapter[];
export function clearAdapters(): void;
```

From src/lib/scraper/pipeline.ts:
```typescript
export function runPipeline(adapters: ScraperAdapter[]): Promise<PipelineRunResult>;
```

From src/lib/scraper/types.ts:
```typescript
export interface PipelineResult {
  sourceId: string;
  sourceName: string;
  recordsScraped: number;
  recordsStored: number;
  errors: string[];
}
export interface PipelineRunResult {
  results: PipelineResult[];
  startedAt: Date;
  completedAt: Date;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Three jurisdiction adapters with Socrata and ArcGIS implementations</name>
  <files>src/lib/scraper/adapters/austin-permits.ts, src/lib/scraper/adapters/dallas-permits.ts, src/lib/scraper/adapters/atlanta-permits.ts, src/lib/scraper/adapters/index.ts, tests/scraper/adapters.test.ts</files>
  <behavior>
    - Test: AustinPermitsAdapter implements ScraperAdapter interface (has sourceId, sourceName, jurisdiction, scrape method)
    - Test: AustinPermitsAdapter.scrape() calls the Socrata SODA endpoint for dataset 3syk-w9eu with $where date filter and $limit
    - Test: AustinPermitsAdapter maps Socrata fields to RawPermitData (permit_number -> permitNumber, permit_location -> address, latitude/longitude preserved)
    - Test: DallasPermitsAdapter implements ScraperAdapter interface
    - Test: DallasPermitsAdapter.scrape() calls the Socrata SODA endpoint for dataset e7gq-4sah
    - Test: DallasPermitsAdapter maps Socrata fields to RawPermitData (permit_number -> permitNumber, street_address -> address, value -> estimatedValue, contractor -> applicantName)
    - Test: DallasPermitsAdapter does NOT provide lat/lng (Dallas dataset lacks coordinates — geocoding needed)
    - Test: AtlantaPermitsAdapter implements ScraperAdapter interface
    - Test: AtlantaPermitsAdapter.scrape() calls the ArcGIS REST API for the building permits feature service
    - Test: All three adapters return arrays of RawPermitData that pass Zod validation
    - Test: initializeAdapters() registers all three adapters in the registry
    - Test: Adapter handles API error (non-200 response) by throwing descriptive error
    - Test: Adapter handles empty result set gracefully (returns empty array)
  </behavior>
  <action>
    1. Create `tests/scraper/adapters.test.ts` — Mock `global.fetch` to return fixture data for each adapter:
       - Create realistic fixture data matching actual Socrata/ArcGIS response shapes (use field names from RESEARCH.md)
       - Test each adapter independently: verify it constructs the correct URL, maps fields correctly, handles errors
       - Test initializeAdapters() registers all three
       - Test pluggability: creating a new adapter implementing ScraperAdapter and registering it works without modifying any existing code

    2. Create `src/lib/scraper/adapters/austin-permits.ts` — AustinPermitsAdapter:
       - sourceId: "austin-tx-permits"
       - sourceName: "City of Austin Issued Construction Permits"
       - jurisdiction: "Austin, TX"
       - Endpoint: `https://data.austintexas.gov/resource/3syk-w9eu.json`
       - SoQL: $where=`issue_date > '{30 days ago}'`, $limit=1000, $order=`issue_date DESC`
       - Field mapping: permit_number -> permitNumber, description -> description, permit_location -> address, permit_type_desc -> projectType, issue_date -> permitDate, latitude -> lat (pass through for geocode skip), longitude -> lng
       - Note: Austin dataset includes latitude/longitude — include them in the returned data so geocoding can be skipped for these records. Add lat/lng as extra properties on the returned RawPermitData or handle in the adapter by including them. Since RawPermitData doesn't have lat/lng, add optional `lat` and `lng` fields to RawPermitData in base-adapter.ts (update the Zod schema from Plan 01).

    3. Create `src/lib/scraper/adapters/dallas-permits.ts` — DallasPermitsAdapter:
       - sourceId: "dallas-tx-permits"
       - sourceName: "City of Dallas Building Permits"
       - jurisdiction: "Dallas, TX"
       - Endpoint: `https://www.dallasopendata.com/resource/e7gq-4sah.json`
       - SoQL: $where=`issued_date > '{30 days ago}'`, $limit=1000, $order=`issued_date DESC`
       - Field mapping: permit_number -> permitNumber, work_description -> description, street_address -> address, permit_type -> projectType, value -> estimatedValue (parse to number), contractor -> applicantName, issued_date -> permitDate
       - Note: Dallas does NOT include lat/lng — these records WILL need geocoding

    4. Create `src/lib/scraper/adapters/atlanta-permits.ts` — AtlantaPermitsAdapter:
       - sourceId: "atlanta-ga-permits"
       - sourceName: "City of Atlanta Building Permits"
       - jurisdiction: "Atlanta, GA"
       - Endpoint: ArcGIS REST API at `https://dpcd-coaplangis.opendata.arcgis.com/api/v3/datasets/655f985f43cc40b4bf2ab7bc73d2169b/downloads/data?format=geojson&spatialRefId=4326` (GeoJSON format download) OR the ArcGIS Feature Service query endpoint if available
       - IMPORTANT: Atlanta's exact API format needs runtime verification (Open Question #1 from research). If the ArcGIS endpoint is unreachable or returns unexpected format during implementation, substitute with a third Socrata city. Document the decision.
       - Map available fields to RawPermitData as closely as possible

    5. Create `src/lib/scraper/adapters/index.ts` — Auto-registration:
       - Import all three adapter classes
       - `initializeAdapters()` function: creates instances, calls registerAdapter() for each
       - Export initializeAdapters

    6. Update `src/lib/scraper/adapters/base-adapter.ts` — Add optional `lat` and `lng` fields to rawPermitSchema so adapters that have source coordinates (Austin) can pass them through for geocoding skip logic.

    RED-GREEN-REFACTOR: Write adapter tests first (mock fetch), then implement adapters to pass tests.
  </action>
  <verify>
    <automated>npx vitest run tests/scraper/adapters.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Three adapters (Austin Socrata, Dallas Socrata, Atlanta ArcGIS) implement ScraperAdapter interface, correctly map source fields to RawPermitData, handle errors gracefully, and register via initializeAdapters(). All adapter tests pass with mocked fetch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Daily scheduler, manual trigger API route, and integration wiring</name>
  <files>src/lib/scraper/scheduler.ts, src/app/api/scraper/run/route.ts, tests/scraper/scheduler.test.ts</files>
  <behavior>
    - Test: startScheduler() calls cron.schedule with '0 6 * * *' expression and UTC timezone
    - Test: Scheduled callback calls initializeAdapters() and runPipeline() with registered adapters
    - Test: stopScheduler() stops the cron task
    - Test: POST /api/scraper/run triggers runPipeline and returns JSON with pipeline results
    - Test: POST /api/scraper/run returns 500 with error message if pipeline throws
  </behavior>
  <action>
    1. Create `tests/scraper/scheduler.test.ts` — Mock node-cron and pipeline:
       - Mock `node-cron` schedule function, capture the callback
       - Verify cron expression is '0 6 * * *' with timezone 'UTC'
       - Verify callback calls pipeline functions
       - Test stopScheduler stops the task

    2. Create `src/lib/scraper/scheduler.ts` — Daily scheduler:
       - Import cron from 'node-cron'
       - Import { runPipeline } from './pipeline'
       - Import { getRegisteredAdapters, clearAdapters } from './registry'
       - Import { initializeAdapters } from './adapters'
       - `startScheduler()`: schedule '0 6 * * *' UTC, callback does: console.log start, initializeAdapters(), runPipeline(getRegisteredAdapters()), log results summary, clearAdapters() after run
       - `stopScheduler()`: stops the cron task
       - Export both functions

    3. Create `src/app/api/scraper/run/route.ts` — Manual trigger API:
       - POST handler (no auth for now — internal/dev use only, add auth in future)
       - Import initializeAdapters, getRegisteredAdapters, clearAdapters, runPipeline
       - On POST: initializeAdapters(), run pipeline, clearAdapters(), return NextResponse.json with PipelineRunResult
       - On error: return 500 with { error: message }
       - Add a comment: "TODO: Add auth guard before production use"

    RED-GREEN-REFACTOR: Write scheduler and API route tests first, then implement.
  </action>
  <verify>
    <automated>npx vitest run tests/scraper/ --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>Scheduler runs pipeline daily at 06:00 UTC. Manual trigger API endpoint at POST /api/scraper/run works. All scraper tests pass (adapters, pipeline, validation, schema, scheduler). Full scraping pipeline is wired end-to-end.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npx vitest run tests/scraper/` — all tests pass (adapters, pipeline, validation, schema, scheduler)
- Three adapters registered and implement ScraperAdapter interface
- Scheduler configured for daily 06:00 UTC execution
- API route at /api/scraper/run responds to POST
- Each adapter maps source-specific fields to RawPermitData correctly
</verification>

<success_criteria>
- Three jurisdiction adapters (Austin, Dallas, Atlanta) fetch permit data from their respective APIs
- Austin adapter preserves source lat/lng for geocoding skip
- Dallas adapter correctly omits lat/lng (triggers geocoding in pipeline)
- Atlanta adapter handles ArcGIS format (or substitutes if endpoint unavailable)
- All adapters auto-register via initializeAdapters()
- Scheduler triggers pipeline daily at 06:00 UTC
- Manual API route triggers pipeline on demand
- Adding a 4th adapter requires only creating a new file + adding to initializeAdapters — no pipeline.ts changes
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-scraping-pipeline/02-02-SUMMARY.md`
</output>
